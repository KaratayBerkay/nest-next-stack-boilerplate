# rabbitmq service (profile: brokers/all) — copy to rabbitmq.env (gitignored).
# Var names must match what the rabbitmq:4-management image expects.
RABBITMQ_DEFAULT_USER=nest
RABBITMQ_DEFAULT_PASS=nest
