# nats service (profile: brokers/all) — configured entirely via the compose
# `command:` (--jetstream --http_port 8222), no environment vars needed.
# Kept for consistency with the other per-service files.
