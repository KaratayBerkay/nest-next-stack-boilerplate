# fluent-bit service — all config comes from the mounted fluent-bit.conf /
# parsers.conf / level_name.lua (see docker-compose.yml volumes:), no
# environment vars needed. Kept for consistency with the other per-service files.
