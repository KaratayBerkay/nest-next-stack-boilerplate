# redis service — no environment vars are currently needed (auth is not
# enabled; `command: redis-server --appendonly yes` handles persistence).
# Kept for consistency with the other per-service files and as a place to add
# e.g. a requirepass arg later. Tracked in git — no secrets here.
