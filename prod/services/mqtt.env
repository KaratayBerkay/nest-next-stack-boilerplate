# mqtt service (profile: brokers/all, eclipse-mosquitto) — no environment vars
# needed; broker config would come from a mounted mosquitto.conf if ever
# customized. Kept for consistency with the other per-service files.
