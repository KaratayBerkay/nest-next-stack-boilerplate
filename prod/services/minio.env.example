# minio + minio-setup services — copy to minio.env (gitignored).
# Also read by `app` under different var names (MINIO_ACCESS_KEY/MINIO_SECRET_KEY)
# — see app.env.example. Keep both in sync if you rotate these.
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=uploads
