import 'package:flutter/material.dart';

class RoutingNavFallback extends StatelessWidget {
  const RoutingNavFallback({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
