export interface LocaleContentProps {
  params: Promise<{ lang: string }>;
}

export interface I18nPageProps {
  params: Promise<{ lang: string }>;
}

export interface GenerateMetadataProps {
  params: Promise<{ lang: string }>;
}
