// AUTO-GENERATED by scripts/generate-pages-sources.ts — do not edit.
// Regenerate with `pnpm generate-pages-sources`.
const sources: Record<string, string> = {"todo-list-1":"\"use client\";\n\nimport { useRef, useState } from \"react\";\nimport { IconPlus, IconTrash } from \"@tabler/icons-react\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { Checkbox } from \"@/components/ui/Checkbox\";\nimport { Input } from \"@/components/ui/Input\";\nimport { IconButton, Button } from \"@/components/ui/Button\";\nimport { Progress } from \"@/components/ui/Progress\";\nimport { cn } from \"@/lib/cn\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ninterface Task {\n  id: string;\n  text: string;\n  done: boolean;\n}\n\nconst SEED = [\n  { id: \"seed-1\", textKey: \"todoList1Task1\", done: false },\n  { id: \"seed-2\", textKey: \"todoList1Task2\", done: true },\n  { id: \"seed-3\", textKey: \"todoList1Task3\", done: false },\n  { id: \"seed-4\", textKey: \"todoList1Task4\", done: false },\n  { id: \"seed-5\", textKey: \"todoList1Task5\", done: true },\n] as const;\n\nexport function SimpleChecklistTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const [tasks, setTasks] = useState<Task[]>(() =>\n    SEED.map((seed) => ({ id: seed.id, text: d[seed.textKey], done: seed.done })),\n  );\n  const [draft, setDraft] = useState(\"\");\n  const nextId = useRef(SEED.length + 1);\n\n  const doneCount = tasks.filter((task) => task.done).length;\n  const percent = tasks.length === 0 ? 0 : Math.round((doneCount / tasks.length) * 100);\n\n  function toggleTask(id: string) {\n    setTasks((prev) =>\n      prev.map((task) => (task.id === id ? { ...task, done: !task.done } : task)),\n    );\n  }\n\n  function deleteTask(id: string) {\n    setTasks((prev) => prev.filter((task) => task.id !== id));\n  }\n\n  function addTask(event: React.FormEvent) {\n    event.preventDefault();\n    const value = draft.trim();\n    if (!value) return;\n    const id = `custom-${nextId.current}`;\n    nextId.current += 1;\n    setTasks((prev) => [...prev, { id, text: value, done: false }]);\n    setDraft(\"\");\n  }\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <CardTitle>{d.todoList1Heading}</CardTitle>\n            <CardDescription>{d.todoList1Subheading}</CardDescription>\n          </CardHeader>\n          <CardContent className=\"flex flex-col gap-4\">\n            <form onSubmit={addTask} className=\"flex items-center gap-2\">\n              <Input\n                value={draft}\n                onChange={(event) => setDraft(event.target.value)}\n                placeholder={d.todoList1InputPlaceholder}\n                aria-label={d.todoList1InputPlaceholder}\n              />\n              <Button type=\"submit\" variant=\"primary\" leftIcon={<IconPlus size={16} />}>\n                {d.todoList1AddButton}\n              </Button>\n            </form>\n\n            {tasks.length === 0 ? (\n              <p className=\"text-muted py-6 text-center text-sm\">{d.todoList1Empty}</p>\n            ) : (\n              <ul className=\"flex flex-col gap-1\">\n                {tasks.map((task) => (\n                  <li\n                    key={task.id}\n                    className={cn(\n                      \"hover:bg-surface-hover flex items-center justify-between gap-2 rounded-md px-2 py-2 transition-opacity\",\n                      task.done && \"opacity-60\",\n                    )}\n                  >\n                    <Checkbox\n                      checked={task.done}\n                      onChange={() => toggleTask(task.id)}\n                      label={task.text}\n                    />\n                    <IconButton\n                      icon={<IconTrash size={15} aria-hidden=\"true\" />}\n                      label={d.todoList1DeleteAria.replace(\"{task}\", task.text)}\n                      variant=\"ghost\"\n                      size=\"icon-sm\"\n                      onClick={() => deleteTask(task.id)}\n                    />\n                  </li>\n                ))}\n              </ul>\n            )}\n          </CardContent>\n          <div className=\"border-border flex items-center gap-3 border-t px-4 py-3 @sm:px-6\">\n            <Progress value={percent} className=\"flex-1\" size=\"sm\" />\n            <span className=\"text-muted shrink-0 text-xs tabular-nums\">\n              {d.todoList1CountLabel\n                .replace(\"{done}\", String(doneCount))\n                .replace(\"{total}\", String(tasks.length))}\n            </span>\n          </div>\n        </Card>\n      </div>\n    </section>\n  );\n}\n","todo-list-2":"\"use client\";\n\nimport { useState } from \"react\";\nimport {\n  IconChevronDown,\n  IconChevronUp,\n  IconGripVertical,\n  IconTrash,\n} from \"@tabler/icons-react\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { Checkbox } from \"@/components/ui/Checkbox\";\nimport { IconButton } from \"@/components/ui/Button\";\nimport { cn } from \"@/lib/cn\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ninterface Task {\n  id: string;\n  text: string;\n  done: boolean;\n}\n\nconst SEED = [\n  { id: \"seed-1\", textKey: \"todoList2Task1\", done: false },\n  { id: \"seed-2\", textKey: \"todoList2Task2\", done: false },\n  { id: \"seed-3\", textKey: \"todoList2Task3\", done: true },\n  { id: \"seed-4\", textKey: \"todoList2Task4\", done: false },\n  { id: \"seed-5\", textKey: \"todoList2Task5\", done: false },\n] as const;\n\nexport function DragHandleReorderTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const [tasks, setTasks] = useState<Task[]>(() =>\n    SEED.map((seed) => ({ id: seed.id, text: d[seed.textKey], done: seed.done })),\n  );\n\n  function toggleTask(id: string) {\n    setTasks((prev) =>\n      prev.map((task) => (task.id === id ? { ...task, done: !task.done } : task)),\n    );\n  }\n\n  function deleteTask(id: string) {\n    setTasks((prev) => prev.filter((task) => task.id !== id));\n  }\n\n  function moveTask(index: number, direction: -1 | 1) {\n    setTasks((prev) => {\n      const target = index + direction;\n      if (target < 0 || target >= prev.length) return prev;\n      const next = [...prev];\n      const [moved] = next.splice(index, 1);\n      next.splice(target, 0, moved);\n      return next;\n    });\n  }\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <CardTitle>{d.todoList2Heading}</CardTitle>\n            <CardDescription>{d.todoList2Subheading}</CardDescription>\n          </CardHeader>\n          <CardContent>\n            <ul className=\"flex flex-col gap-1\">\n              {tasks.map((task, index) => (\n                <li\n                  key={task.id}\n                  className={cn(\n                    \"border-border bg-surface flex items-center gap-2 rounded-md border px-2 py-2\",\n                    task.done && \"opacity-60\",\n                  )}\n                >\n                  <IconGripVertical\n                    size={16}\n                    aria-hidden=\"true\"\n                    className=\"text-muted shrink-0 cursor-grab\"\n                  />\n                  <div className=\"min-w-0 flex-1\">\n                    <Checkbox\n                      checked={task.done}\n                      onChange={() => toggleTask(task.id)}\n                      label={task.text}\n                    />\n                  </div>\n                  <div className=\"flex shrink-0 items-center gap-0.5\">\n                    <IconButton\n                      icon={<IconChevronUp size={15} aria-hidden=\"true\" />}\n                      label={d.todoList2MoveUpAria.replace(\"{task}\", task.text)}\n                      variant=\"ghost\"\n                      size=\"icon-xs\"\n                      disabled={index === 0}\n                      onClick={() => moveTask(index, -1)}\n                    />\n                    <IconButton\n                      icon={<IconChevronDown size={15} aria-hidden=\"true\" />}\n                      label={d.todoList2MoveDownAria.replace(\"{task}\", task.text)}\n                      variant=\"ghost\"\n                      size=\"icon-xs\"\n                      disabled={index === tasks.length - 1}\n                      onClick={() => moveTask(index, 1)}\n                    />\n                    <IconButton\n                      icon={<IconTrash size={15} aria-hidden=\"true\" />}\n                      label={d.todoList2DeleteAria.replace(\"{task}\", task.text)}\n                      variant=\"ghost\"\n                      size=\"icon-xs\"\n                      onClick={() => deleteTask(task.id)}\n                    />\n                  </div>\n                </li>\n              ))}\n            </ul>\n            {tasks.length === 0 && (\n              <p className=\"text-muted py-6 text-center text-sm\">{d.todoList2Empty}</p>\n            )}\n          </CardContent>\n        </Card>\n      </div>\n    </section>\n  );\n}\n","todo-list-3":"\"use client\";\n\nimport { useRef, useState } from \"react\";\nimport { IconPlus, IconTrash } from \"@tabler/icons-react\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { Checkbox } from \"@/components/ui/Checkbox\";\nimport { Input } from \"@/components/ui/Input\";\nimport { IconButton } from \"@/components/ui/Button\";\nimport { Badge } from \"@/components/ui/Badge\";\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from \"@/components/ui/Tabs\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ninterface Task {\n  id: string;\n  text: string;\n  done: boolean;\n}\n\nconst SEED = [\n  { id: \"seed-1\", textKey: \"todoList3Task1\", done: false },\n  { id: \"seed-2\", textKey: \"todoList3Task2\", done: false },\n  { id: \"seed-3\", textKey: \"todoList3Task3\", done: true },\n  { id: \"seed-4\", textKey: \"todoList3Task4\", done: true },\n  { id: \"seed-5\", textKey: \"todoList3Task5\", done: false },\n  { id: \"seed-6\", textKey: \"todoList3Task6\", done: true },\n] as const;\n\nfunction TaskRow({\n  task,\n  onToggle,\n  onDelete,\n  deleteLabel,\n}: {\n  task: Task;\n  onToggle: () => void;\n  onDelete: () => void;\n  deleteLabel: string;\n}) {\n  return (\n    <li className=\"hover:bg-surface-hover flex items-center justify-between gap-2 rounded-md px-2 py-2\">\n      <Checkbox checked={task.done} onChange={onToggle} label={task.text} />\n      <IconButton\n        icon={<IconTrash size={15} aria-hidden=\"true\" />}\n        label={deleteLabel}\n        variant=\"ghost\"\n        size=\"icon-sm\"\n        onClick={onDelete}\n      />\n    </li>\n  );\n}\n\nexport function TabbedStatusTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const [tasks, setTasks] = useState<Task[]>(() =>\n    SEED.map((seed) => ({ id: seed.id, text: d[seed.textKey], done: seed.done })),\n  );\n  const [draft, setDraft] = useState(\"\");\n  const nextId = useRef(SEED.length + 1);\n\n  const active = tasks.filter((task) => !task.done);\n  const completed = tasks.filter((task) => task.done);\n\n  function toggleTask(id: string) {\n    setTasks((prev) =>\n      prev.map((task) => (task.id === id ? { ...task, done: !task.done } : task)),\n    );\n  }\n\n  function deleteTask(id: string) {\n    setTasks((prev) => prev.filter((task) => task.id !== id));\n  }\n\n  function addTask(event: React.FormEvent) {\n    event.preventDefault();\n    const value = draft.trim();\n    if (!value) return;\n    const id = `custom-${nextId.current}`;\n    nextId.current += 1;\n    setTasks((prev) => [...prev, { id, text: value, done: false }]);\n    setDraft(\"\");\n  }\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <CardTitle>{d.todoList3Heading}</CardTitle>\n            <CardDescription>{d.todoList3Subheading}</CardDescription>\n          </CardHeader>\n          <CardContent className=\"flex flex-col gap-4\">\n            <form onSubmit={addTask} className=\"flex items-center gap-2\">\n              <Input\n                value={draft}\n                onChange={(event) => setDraft(event.target.value)}\n                placeholder={d.todoList3InputPlaceholder}\n                aria-label={d.todoList3InputPlaceholder}\n              />\n              <IconButton\n                type=\"submit\"\n                icon={<IconPlus size={16} aria-hidden=\"true\" />}\n                label={d.todoList3AddAria}\n                variant=\"primary\"\n                size=\"icon\"\n              />\n            </form>\n\n            <Tabs defaultValue=\"all\">\n              <TabsList>\n                <TabsTrigger value=\"all\" className=\"gap-1.5\">\n                  {d.todoList3TabAll}\n                  <Badge variant=\"secondary\" size=\"sm\">\n                    {tasks.length}\n                  </Badge>\n                </TabsTrigger>\n                <TabsTrigger value=\"active\" className=\"gap-1.5\">\n                  {d.todoList3TabActive}\n                  <Badge variant=\"secondary\" size=\"sm\">\n                    {active.length}\n                  </Badge>\n                </TabsTrigger>\n                <TabsTrigger value=\"completed\" className=\"gap-1.5\">\n                  {d.todoList3TabCompleted}\n                  <Badge variant=\"secondary\" size=\"sm\">\n                    {completed.length}\n                  </Badge>\n                </TabsTrigger>\n              </TabsList>\n\n              <TabsContent value=\"all\" className=\"mt-3\">\n                <ul className=\"flex flex-col gap-1\">\n                  {tasks.map((task) => (\n                    <TaskRow\n                      key={task.id}\n                      task={task}\n                      onToggle={() => toggleTask(task.id)}\n                      onDelete={() => deleteTask(task.id)}\n                      deleteLabel={d.todoList3DeleteAria.replace(\"{task}\", task.text)}\n                    />\n                  ))}\n                </ul>\n              </TabsContent>\n              <TabsContent value=\"active\" className=\"mt-3\">\n                {active.length === 0 ? (\n                  <p className=\"text-muted py-6 text-center text-sm\">\n                    {d.todoList3EmptyActive}\n                  </p>\n                ) : (\n                  <ul className=\"flex flex-col gap-1\">\n                    {active.map((task) => (\n                      <TaskRow\n                        key={task.id}\n                        task={task}\n                        onToggle={() => toggleTask(task.id)}\n                        onDelete={() => deleteTask(task.id)}\n                        deleteLabel={d.todoList3DeleteAria.replace(\"{task}\", task.text)}\n                      />\n                    ))}\n                  </ul>\n                )}\n              </TabsContent>\n              <TabsContent value=\"completed\" className=\"mt-3\">\n                {completed.length === 0 ? (\n                  <p className=\"text-muted py-6 text-center text-sm\">\n                    {d.todoList3EmptyCompleted}\n                  </p>\n                ) : (\n                  <ul className=\"flex flex-col gap-1\">\n                    {completed.map((task) => (\n                      <TaskRow\n                        key={task.id}\n                        task={task}\n                        onToggle={() => toggleTask(task.id)}\n                        onDelete={() => deleteTask(task.id)}\n                        deleteLabel={d.todoList3DeleteAria.replace(\"{task}\", task.text)}\n                      />\n                    ))}\n                  </ul>\n                )}\n              </TabsContent>\n            </Tabs>\n          </CardContent>\n        </Card>\n      </div>\n    </section>\n  );\n}\n","todo-list-4":"\"use client\";\n\nimport { useRef, useState } from \"react\";\nimport { IconPlus, IconTrash } from \"@tabler/icons-react\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { Checkbox } from \"@/components/ui/Checkbox\";\nimport { Input } from \"@/components/ui/Input\";\nimport { IconButton, Button } from \"@/components/ui/Button\";\nimport { Badge } from \"@/components/ui/Badge\";\nimport { Progress } from \"@/components/ui/Progress\";\nimport { ToggleGroup, ToggleGroupItem } from \"@/components/ui/toggle-group\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ntype Priority = \"low\" | \"medium\" | \"high\";\n\ninterface Task {\n  id: string;\n  text: string;\n  done: boolean;\n  priority: Priority;\n}\n\nconst SEED = [\n  { id: \"seed-1\", textKey: \"todoList4Task1\", done: false, priority: \"high\" },\n  { id: \"seed-2\", textKey: \"todoList4Task2\", done: false, priority: \"medium\" },\n  { id: \"seed-3\", textKey: \"todoList4Task3\", done: true, priority: \"low\" },\n  { id: \"seed-4\", textKey: \"todoList4Task4\", done: false, priority: \"medium\" },\n  { id: \"seed-5\", textKey: \"todoList4Task5\", done: true, priority: \"high\" },\n] as const;\n\nconst PRIORITY_BADGE: Record<Priority, \"secondary\" | \"warning\" | \"error\"> = {\n  low: \"secondary\",\n  medium: \"warning\",\n  high: \"error\",\n};\n\nexport function ProgressComposerTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const PRIORITY_LABEL: Record<Priority, string> = {\n    low: d.todoList4PriorityLow,\n    medium: d.todoList4PriorityMedium,\n    high: d.todoList4PriorityHigh,\n  };\n  const [tasks, setTasks] = useState<Task[]>(() =>\n    SEED.map((seed) => ({\n      id: seed.id,\n      text: d[seed.textKey],\n      done: seed.done,\n      priority: seed.priority,\n    })),\n  );\n  const [draft, setDraft] = useState(\"\");\n  const [draftPriority, setDraftPriority] = useState<Priority>(\"medium\");\n  const nextId = useRef(SEED.length + 1);\n\n  const doneCount = tasks.filter((task) => task.done).length;\n  const percent = tasks.length === 0 ? 0 : Math.round((doneCount / tasks.length) * 100);\n\n  function toggleTask(id: string) {\n    setTasks((prev) =>\n      prev.map((task) => (task.id === id ? { ...task, done: !task.done } : task)),\n    );\n  }\n\n  function deleteTask(id: string) {\n    setTasks((prev) => prev.filter((task) => task.id !== id));\n  }\n\n  function addTask(event: React.FormEvent) {\n    event.preventDefault();\n    const value = draft.trim();\n    if (!value) return;\n    const id = `custom-${nextId.current}`;\n    nextId.current += 1;\n    setTasks((prev) => [...prev, { id, text: value, done: false, priority: draftPriority }]);\n    setDraft(\"\");\n  }\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <div className=\"flex items-start justify-between gap-4\">\n              <div className=\"flex flex-col gap-1\">\n                <CardTitle>{d.todoList4Heading}</CardTitle>\n                <CardDescription>{d.todoList4Subheading}</CardDescription>\n              </div>\n              <span className=\"text-brand shrink-0 text-2xl font-bold tabular-nums\">\n                {percent}%\n              </span>\n            </div>\n            <Progress value={percent} size=\"lg\" className=\"mt-2\" />\n            <span className=\"text-muted text-xs\">\n              {d.todoList4StatsLabel\n                .replace(\"{done}\", String(doneCount))\n                .replace(\"{total}\", String(tasks.length))}\n            </span>\n          </CardHeader>\n          <CardContent className=\"flex flex-col gap-4\">\n            <form onSubmit={addTask} className=\"border-border flex flex-col gap-2 rounded-lg border border-dashed p-3\">\n              <Input\n                value={draft}\n                onChange={(event) => setDraft(event.target.value)}\n                placeholder={d.todoList4InputPlaceholder}\n                aria-label={d.todoList4InputPlaceholder}\n              />\n              <div className=\"flex items-center justify-between gap-2\">\n                <ToggleGroup\n                  type=\"single\"\n                  value={draftPriority}\n                  onValueChange={(value) => {\n                    if (value) setDraftPriority(value as Priority);\n                  }}\n                  aria-label={d.todoList4PriorityGroupAria}\n                >\n                  <ToggleGroupItem value=\"low\" size=\"sm\">\n                    {d.todoList4PriorityLow}\n                  </ToggleGroupItem>\n                  <ToggleGroupItem value=\"medium\" size=\"sm\">\n                    {d.todoList4PriorityMedium}\n                  </ToggleGroupItem>\n                  <ToggleGroupItem value=\"high\" size=\"sm\">\n                    {d.todoList4PriorityHigh}\n                  </ToggleGroupItem>\n                </ToggleGroup>\n                <Button type=\"submit\" variant=\"primary\" size=\"sm\" leftIcon={<IconPlus size={15} />}>\n                  {d.todoList4AddButton}\n                </Button>\n              </div>\n            </form>\n\n            <ul className=\"flex flex-col gap-1\">\n              {tasks.map((task) => (\n                <li\n                  key={task.id}\n                  className=\"hover:bg-surface-hover flex items-center justify-between gap-2 rounded-md px-2 py-2\"\n                >\n                  <div className=\"flex min-w-0 items-center gap-2\">\n                    <Checkbox checked={task.done} onChange={() => toggleTask(task.id)} label={task.text} />\n                  </div>\n                  <div className=\"flex shrink-0 items-center gap-2\">\n                    <Badge variant={PRIORITY_BADGE[task.priority]} size=\"sm\">\n                      {PRIORITY_LABEL[task.priority]}\n                    </Badge>\n                    <IconButton\n                      icon={<IconTrash size={15} aria-hidden=\"true\" />}\n                      label={d.todoList4DeleteAria.replace(\"{task}\", task.text)}\n                      variant=\"ghost\"\n                      size=\"icon-sm\"\n                      onClick={() => deleteTask(task.id)}\n                    />\n                  </div>\n                </li>\n              ))}\n            </ul>\n          </CardContent>\n        </Card>\n      </div>\n    </section>\n  );\n}\n","todo-list-5":"\"use client\";\n\nimport { useState } from \"react\";\nimport { IconCheck, IconPencil, IconTrash, IconX } from \"@tabler/icons-react\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { Checkbox } from \"@/components/ui/Checkbox\";\nimport { Input } from \"@/components/ui/Input\";\nimport { IconButton } from \"@/components/ui/Button\";\nimport { cn } from \"@/lib/cn\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ninterface Task {\n  id: string;\n  text: string;\n  done: boolean;\n}\n\nconst SEED = [\n  { id: \"seed-1\", textKey: \"todoList5Task1\", done: false },\n  { id: \"seed-2\", textKey: \"todoList5Task2\", done: true },\n  { id: \"seed-3\", textKey: \"todoList5Task3\", done: false },\n  { id: \"seed-4\", textKey: \"todoList5Task4\", done: false },\n  { id: \"seed-5\", textKey: \"todoList5Task5\", done: false },\n] as const;\n\nexport function SwipeActionRowTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const [tasks, setTasks] = useState<Task[]>(() =>\n    SEED.map((seed) => ({ id: seed.id, text: d[seed.textKey], done: seed.done })),\n  );\n  const [editingId, setEditingId] = useState<string | null>(null);\n  const [draftText, setDraftText] = useState(\"\");\n\n  function toggleTask(id: string) {\n    setTasks((prev) =>\n      prev.map((task) => (task.id === id ? { ...task, done: !task.done } : task)),\n    );\n  }\n\n  function deleteTask(id: string) {\n    setTasks((prev) => prev.filter((task) => task.id !== id));\n    if (editingId === id) setEditingId(null);\n  }\n\n  function startEdit(task: Task) {\n    setEditingId(task.id);\n    setDraftText(task.text);\n  }\n\n  function cancelEdit() {\n    setEditingId(null);\n    setDraftText(\"\");\n  }\n\n  function saveEdit(id: string) {\n    const value = draftText.trim();\n    if (value) {\n      setTasks((prev) =>\n        prev.map((task) => (task.id === id ? { ...task, text: value } : task)),\n      );\n    }\n    setEditingId(null);\n    setDraftText(\"\");\n  }\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <CardTitle>{d.todoList5Heading}</CardTitle>\n            <CardDescription>{d.todoList5Subheading}</CardDescription>\n          </CardHeader>\n          <CardContent>\n            <ul className=\"flex flex-col gap-1\">\n              {tasks.map((task) => {\n                const isEditing = editingId === task.id;\n                return (\n                  <li\n                    key={task.id}\n                    className={cn(\n                      \"group flex items-center gap-2 rounded-md border-l-4 py-2 pr-2 pl-2.5 transition-colors\",\n                      task.done\n                        ? \"border-success bg-success/5\"\n                        : \"border-transparent hover:bg-surface-hover\",\n                    )}\n                  >\n                    <Checkbox\n                      checked={task.done}\n                      onChange={() => toggleTask(task.id)}\n                      aria-label={d.todoList5ToggleAria.replace(\"{task}\", task.text)}\n                    />\n                    {isEditing ? (\n                      <div className=\"flex flex-1 items-center gap-1\">\n                        <Input\n                          value={draftText}\n                          onChange={(event) => setDraftText(event.target.value)}\n                          aria-label={d.todoList5EditInputAria}\n                        />\n                        <IconButton\n                          icon={<IconCheck size={15} aria-hidden=\"true\" />}\n                          label={d.todoList5SaveAria}\n                          variant=\"ghost\"\n                          size=\"icon-sm\"\n                          onClick={() => saveEdit(task.id)}\n                        />\n                        <IconButton\n                          icon={<IconX size={15} aria-hidden=\"true\" />}\n                          label={d.todoList5CancelAria}\n                          variant=\"ghost\"\n                          size=\"icon-sm\"\n                          onClick={cancelEdit}\n                        />\n                      </div>\n                    ) : (\n                      <>\n                        <span\n                          className={cn(\n                            \"flex-1 truncate text-sm\",\n                            task.done ? \"text-muted line-through\" : \"text-fg\",\n                          )}\n                        >\n                          {task.text}\n                        </span>\n                        <div className=\"flex shrink-0 items-center gap-0.5 opacity-0 transition-opacity group-focus-within:opacity-100 group-hover:opacity-100\">\n                          <IconButton\n                            icon={<IconPencil size={15} aria-hidden=\"true\" />}\n                            label={d.todoList5EditAria.replace(\"{task}\", task.text)}\n                            variant=\"ghost\"\n                            size=\"icon-sm\"\n                            onClick={() => startEdit(task)}\n                          />\n                          <IconButton\n                            icon={<IconTrash size={15} aria-hidden=\"true\" />}\n                            label={d.todoList5DeleteAria.replace(\"{task}\", task.text)}\n                            variant=\"ghost\"\n                            size=\"icon-sm\"\n                            onClick={() => deleteTask(task.id)}\n                          />\n                        </div>\n                      </>\n                    )}\n                  </li>\n                );\n              })}\n            </ul>\n            {tasks.length === 0 && (\n              <p className=\"text-muted py-6 text-center text-sm\">{d.todoList5Empty}</p>\n            )}\n          </CardContent>\n        </Card>\n      </div>\n    </section>\n  );\n}\n","todo-list-6":"\"use client\";\n\nimport { useState } from \"react\";\nimport { IconTrash } from \"@tabler/icons-react\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { Checkbox } from \"@/components/ui/Checkbox\";\nimport { IconButton } from \"@/components/ui/Button\";\nimport { Badge } from \"@/components/ui/Badge\";\nimport { ToggleGroup, ToggleGroupItem } from \"@/components/ui/toggle-group\";\nimport { cn } from \"@/lib/cn\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ntype Priority = \"high\" | \"medium\" | \"low\";\ntype Filter = \"all\" | Priority;\n\ninterface Task {\n  id: string;\n  text: string;\n  done: boolean;\n  priority: Priority;\n}\n\nconst SEED = [\n  { id: \"seed-1\", textKey: \"todoList6Task1\", done: false, priority: \"high\" },\n  { id: \"seed-2\", textKey: \"todoList6Task2\", done: false, priority: \"high\" },\n  { id: \"seed-3\", textKey: \"todoList6Task3\", done: true, priority: \"medium\" },\n  { id: \"seed-4\", textKey: \"todoList6Task4\", done: false, priority: \"medium\" },\n  { id: \"seed-5\", textKey: \"todoList6Task5\", done: false, priority: \"low\" },\n  { id: \"seed-6\", textKey: \"todoList6Task6\", done: true, priority: \"low\" },\n] as const;\n\nconst PRIORITY_ORDER: Priority[] = [\"high\", \"medium\", \"low\"];\n\nconst PRIORITY_DOT: Record<Priority, string> = {\n  high: \"bg-error\",\n  medium: \"bg-warning\",\n  low: \"bg-info\",\n};\n\nconst PRIORITY_BADGE: Record<Priority, \"error\" | \"warning\" | \"info\"> = {\n  high: \"error\",\n  medium: \"warning\",\n  low: \"info\",\n};\n\nexport function PriorityGroupedTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const PRIORITY_LABEL: Record<Priority, string> = {\n    high: d.todoList6FilterHigh,\n    medium: d.todoList6FilterMedium,\n    low: d.todoList6FilterLow,\n  };\n  const SECTION_LABEL: Record<Priority, string> = {\n    high: d.todoList6SectionHigh,\n    medium: d.todoList6SectionMedium,\n    low: d.todoList6SectionLow,\n  };\n  const [tasks, setTasks] = useState<Task[]>(() =>\n    SEED.map((seed) => ({\n      id: seed.id,\n      text: d[seed.textKey],\n      done: seed.done,\n      priority: seed.priority,\n    })),\n  );\n  const [filter, setFilter] = useState<Filter>(\"all\");\n\n  function toggleTask(id: string) {\n    setTasks((prev) =>\n      prev.map((task) => (task.id === id ? { ...task, done: !task.done } : task)),\n    );\n  }\n\n  function deleteTask(id: string) {\n    setTasks((prev) => prev.filter((task) => task.id !== id));\n  }\n\n  const visibleGroups =\n    filter === \"all\" ? PRIORITY_ORDER : PRIORITY_ORDER.filter((p) => p === filter);\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-2xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <div className=\"flex flex-wrap items-start justify-between gap-4\">\n              <div className=\"flex flex-col gap-1\">\n                <CardTitle>{d.todoList6Heading}</CardTitle>\n                <CardDescription>{d.todoList6Subheading}</CardDescription>\n              </div>\n              <ToggleGroup\n                type=\"single\"\n                value={filter}\n                onValueChange={(value) => {\n                  if (value) setFilter(value as Filter);\n                }}\n                aria-label={d.todoList6FilterGroupAria}\n              >\n                <ToggleGroupItem value=\"all\" size=\"sm\">\n                  {d.todoList6FilterAll}\n                </ToggleGroupItem>\n                {PRIORITY_ORDER.map((priority) => (\n                  <ToggleGroupItem key={priority} value={priority} size=\"sm\" className=\"gap-1.5\">\n                    <span\n                      className={cn(\"size-1.5 rounded-full\", PRIORITY_DOT[priority])}\n                      aria-hidden=\"true\"\n                    />\n                    {PRIORITY_LABEL[priority]}\n                  </ToggleGroupItem>\n                ))}\n              </ToggleGroup>\n            </div>\n          </CardHeader>\n          <CardContent className=\"flex flex-col gap-5\">\n            {visibleGroups.map((priority) => {\n              const groupTasks = tasks.filter((task) => task.priority === priority);\n              return (\n                <div key={priority} className=\"flex flex-col gap-1.5\">\n                  <div className=\"flex items-center gap-2\">\n                    <Badge variant={PRIORITY_BADGE[priority]} size=\"sm\">\n                      {SECTION_LABEL[priority]}\n                    </Badge>\n                    <span className=\"text-muted text-xs tabular-nums\">{groupTasks.length}</span>\n                  </div>\n                  {groupTasks.length === 0 ? (\n                    <p className=\"text-muted px-2 py-1.5 text-sm\">{d.todoList6EmptyGroup}</p>\n                  ) : (\n                    <ul className=\"flex flex-col gap-0.5\">\n                      {groupTasks.map((task) => (\n                        <li\n                          key={task.id}\n                          className={cn(\n                            \"hover:bg-surface-hover flex items-center justify-between gap-2 rounded-md px-2 py-1.5\",\n                            task.done && \"opacity-60\",\n                          )}\n                        >\n                          <Checkbox\n                            checked={task.done}\n                            onChange={() => toggleTask(task.id)}\n                            label={task.text}\n                            size=\"sm\"\n                          />\n                          <IconButton\n                            icon={<IconTrash size={14} aria-hidden=\"true\" />}\n                            label={d.todoList6DeleteAria.replace(\"{task}\", task.text)}\n                            variant=\"ghost\"\n                            size=\"icon-xs\"\n                            onClick={() => deleteTask(task.id)}\n                          />\n                        </li>\n                      ))}\n                    </ul>\n                  )}\n                </div>\n              );\n            })}\n          </CardContent>\n        </Card>\n      </div>\n    </section>\n  );\n}\n","todo-list-7":"\"use client\";\n\nimport { useState } from \"react\";\nimport { IconTrash } from \"@tabler/icons-react\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { Checkbox } from \"@/components/ui/Checkbox\";\nimport { IconButton } from \"@/components/ui/Button\";\nimport { Badge } from \"@/components/ui/Badge\";\nimport { cn } from \"@/lib/cn\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ninterface Task {\n  id: string;\n  text: string;\n  done: boolean;\n  day: number;\n  project: string;\n}\n\nconst DAYS = [\n  { index: 0, labelKey: \"todoList7Day0\", number: 9 },\n  { index: 1, labelKey: \"todoList7Day1\", number: 10 },\n  { index: 2, labelKey: \"todoList7Day2\", number: 11 },\n  { index: 3, labelKey: \"todoList7Day3\", number: 12 },\n  { index: 4, labelKey: \"todoList7Day4\", number: 13 },\n  { index: 5, labelKey: \"todoList7Day5\", number: 14 },\n  { index: 6, labelKey: \"todoList7Day6\", number: 15 },\n] as const;\n\nconst SEED = [\n  { id: \"seed-1\", textKey: \"todoList7Task1\", done: false, day: 1, projectKey: \"todoList7Project1\" },\n  { id: \"seed-2\", textKey: \"todoList7Task2\", done: false, day: 1, projectKey: \"todoList7Project2\" },\n  { id: \"seed-3\", textKey: \"todoList7Task3\", done: true, day: 2, projectKey: \"todoList7Project1\" },\n  { id: \"seed-4\", textKey: \"todoList7Task4\", done: false, day: 3, projectKey: \"todoList7Project3\" },\n  { id: \"seed-5\", textKey: \"todoList7Task5\", done: false, day: 3, projectKey: \"todoList7Project2\" },\n  { id: \"seed-6\", textKey: \"todoList7Task6\", done: false, day: 4, projectKey: \"todoList7Project1\" },\n] as const;\n\nexport function CalendarDayTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const [tasks, setTasks] = useState<Task[]>(() =>\n    SEED.map((seed) => ({\n      id: seed.id,\n      text: d[seed.textKey],\n      done: seed.done,\n      day: seed.day,\n      project: d[seed.projectKey],\n    })),\n  );\n  const [selectedDay, setSelectedDay] = useState<number>(1);\n\n  function toggleTask(id: string) {\n    setTasks((prev) =>\n      prev.map((task) => (task.id === id ? { ...task, done: !task.done } : task)),\n    );\n  }\n\n  function deleteTask(id: string) {\n    setTasks((prev) => prev.filter((task) => task.id !== id));\n  }\n\n  const dayTasks = tasks.filter((task) => task.day === selectedDay);\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-2xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <CardTitle>{d.todoList7Heading}</CardTitle>\n            <CardDescription>{d.todoList7Subheading}</CardDescription>\n          </CardHeader>\n          <CardContent className=\"flex flex-col gap-4\">\n            <div className=\"grid grid-cols-7 gap-1.5\">\n              {DAYS.map((day) => {\n                const dayLabel = d[day.labelKey];\n                const isSelected = selectedDay === day.index;\n                const count = tasks.filter((task) => task.day === day.index).length;\n                return (\n                  <button\n                    key={day.index}\n                    type=\"button\"\n                    aria-pressed={isSelected}\n                    aria-label={d.todoList7DaySelectAria.replace(\"{day}\", dayLabel)}\n                    onClick={() => setSelectedDay(day.index)}\n                    className={cn(\n                      \"flex flex-col items-center gap-1 rounded-lg border py-2 transition-colors\",\n                      isSelected\n                        ? \"border-brand bg-brand text-brand-fg\"\n                        : \"border-border hover:bg-surface-hover text-fg\",\n                    )}\n                  >\n                    <span className=\"text-[10px] font-medium tracking-wide uppercase opacity-80\">\n                      {dayLabel}\n                    </span>\n                    <span className=\"text-sm font-semibold tabular-nums\">{day.number}</span>\n                    {count > 0 && (\n                      <span\n                        className={cn(\n                          \"size-1.5 rounded-full\",\n                          isSelected ? \"bg-brand-fg\" : \"bg-brand\",\n                        )}\n                        aria-hidden=\"true\"\n                      />\n                    )}\n                  </button>\n                );\n              })}\n            </div>\n\n            {dayTasks.length === 0 ? (\n              <p className=\"text-muted py-6 text-center text-sm\">{d.todoList7Empty}</p>\n            ) : (\n              <ul className=\"flex flex-col gap-1\">\n                {dayTasks.map((task) => (\n                  <li\n                    key={task.id}\n                    className={cn(\n                      \"hover:bg-surface-hover flex items-center justify-between gap-2 rounded-md px-2 py-2\",\n                      task.done && \"opacity-60\",\n                    )}\n                  >\n                    <Checkbox checked={task.done} onChange={() => toggleTask(task.id)} label={task.text} />\n                    <div className=\"flex shrink-0 items-center gap-2\">\n                      <Badge variant=\"outline\" size=\"sm\">\n                        {task.project}\n                      </Badge>\n                      <IconButton\n                        icon={<IconTrash size={15} aria-hidden=\"true\" />}\n                        label={d.todoList7DeleteAria.replace(\"{task}\", task.text)}\n                        variant=\"ghost\"\n                        size=\"icon-sm\"\n                        onClick={() => deleteTask(task.id)}\n                      />\n                    </div>\n                  </li>\n                ))}\n              </ul>\n            )}\n          </CardContent>\n        </Card>\n      </div>\n    </section>\n  );\n}\n","todo-list-8":"\"use client\";\n\nimport { useState } from \"react\";\nimport { IconChevronDown } from \"@tabler/icons-react\";\nimport {\n  Accordion,\n  AccordionContent,\n  AccordionItem,\n  AccordionTrigger,\n} from \"@/components/ui/Accordion\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { Checkbox, IndeterminateCheckbox } from \"@/components/ui/Checkbox\";\nimport { Badge } from \"@/components/ui/Badge\";\nimport { cn } from \"@/lib/cn\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ninterface Subtask {\n  id: string;\n  text: string;\n  done: boolean;\n}\n\ninterface ParentTask {\n  id: string;\n  text: string;\n  subtasks: Subtask[];\n}\n\nconst SEED = [\n  {\n    id: \"seed-1\",\n    textKey: \"todoList8Task1\",\n    subtasks: [\n      { id: \"seed-1-1\", textKey: \"todoList8Task1Sub1\", done: true },\n      { id: \"seed-1-2\", textKey: \"todoList8Task1Sub2\", done: true },\n      { id: \"seed-1-3\", textKey: \"todoList8Task1Sub3\", done: false },\n    ],\n  },\n  {\n    id: \"seed-2\",\n    textKey: \"todoList8Task2\",\n    subtasks: [\n      { id: \"seed-2-1\", textKey: \"todoList8Task2Sub1\", done: true },\n      { id: \"seed-2-2\", textKey: \"todoList8Task2Sub2\", done: true },\n    ],\n  },\n  {\n    id: \"seed-3\",\n    textKey: \"todoList8Task3\",\n    subtasks: [\n      { id: \"seed-3-1\", textKey: \"todoList8Task3Sub1\", done: false },\n      { id: \"seed-3-2\", textKey: \"todoList8Task3Sub2\", done: false },\n      { id: \"seed-3-3\", textKey: \"todoList8Task3Sub3\", done: false },\n    ],\n  },\n  {\n    id: \"seed-4\",\n    textKey: \"todoList8Task4\",\n    subtasks: [\n      { id: \"seed-4-1\", textKey: \"todoList8Task4Sub1\", done: true },\n      { id: \"seed-4-2\", textKey: \"todoList8Task4Sub2\", done: false },\n    ],\n  },\n] as const;\n\nexport function NestedSubtasksTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const [tasks, setTasks] = useState<ParentTask[]>(() =>\n    SEED.map((seed) => ({\n      id: seed.id,\n      text: d[seed.textKey],\n      subtasks: seed.subtasks.map((sub) => ({\n        id: sub.id,\n        text: d[sub.textKey],\n        done: sub.done,\n      })),\n    })),\n  );\n\n  function toggleSubtask(taskId: string, subId: string) {\n    setTasks((prev) =>\n      prev.map((task) =>\n        task.id !== taskId\n          ? task\n          : {\n              ...task,\n              subtasks: task.subtasks.map((sub) =>\n                sub.id === subId ? { ...sub, done: !sub.done } : sub,\n              ),\n            },\n      ),\n    );\n  }\n\n  function toggleAllSubtasks(taskId: string) {\n    setTasks((prev) =>\n      prev.map((task) => {\n        if (task.id !== taskId) return task;\n        const allDone = task.subtasks.every((sub) => sub.done);\n        return {\n          ...task,\n          subtasks: task.subtasks.map((sub) => ({ ...sub, done: !allDone })),\n        };\n      }),\n    );\n  }\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <CardTitle>{d.todoList8Heading}</CardTitle>\n            <CardDescription>{d.todoList8Subheading}</CardDescription>\n          </CardHeader>\n          <CardContent>\n            <Accordion type=\"single\" collapsible>\n              {tasks.map((task) => {\n                const doneCount = task.subtasks.filter((sub) => sub.done).length;\n                const allDone = doneCount === task.subtasks.length;\n                const someDone = doneCount > 0;\n                return (\n                  <AccordionItem key={task.id} value={task.id}>\n                    <div className=\"flex items-center gap-1 pr-3\">\n                      <span className=\"pl-4\">\n                        <IndeterminateCheckbox\n                          checked={allDone}\n                          indeterminate={someDone && !allDone}\n                          onChange={() => toggleAllSubtasks(task.id)}\n                          aria-label={d.todoList8ToggleAllAria.replace(\"{task}\", task.text)}\n                        />\n                      </span>\n                      <AccordionTrigger className=\"group gap-3\">\n                        <span\n                          className={cn(\n                            \"flex-1 text-left text-sm font-medium\",\n                            allDone && \"text-muted line-through\",\n                          )}\n                        >\n                          {task.text}\n                        </span>\n                        <span className=\"flex items-center gap-2\">\n                          <Badge variant=\"secondary\" size=\"sm\">\n                            {doneCount}/{task.subtasks.length}\n                          </Badge>\n                          <IconChevronDown\n                            size={16}\n                            className=\"text-muted shrink-0 transition-transform duration-200 group-data-[state=open]:rotate-180\"\n                            aria-hidden=\"true\"\n                          />\n                        </span>\n                      </AccordionTrigger>\n                    </div>\n                    <AccordionContent>\n                      <ul className=\"flex flex-col gap-1.5 pl-9\">\n                        {task.subtasks.map((sub) => (\n                          <li key={sub.id}>\n                            <Checkbox\n                              size=\"sm\"\n                              checked={sub.done}\n                              onChange={() => toggleSubtask(task.id, sub.id)}\n                              label={sub.text}\n                            />\n                          </li>\n                        ))}\n                      </ul>\n                    </AccordionContent>\n                  </AccordionItem>\n                );\n              })}\n            </Accordion>\n          </CardContent>\n        </Card>\n      </div>\n    </section>\n  );\n}\n","todo-list-9":"\"use client\";\n\nimport { useMemo, useState } from \"react\";\nimport { IconSearch, IconTrash } from \"@tabler/icons-react\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { Checkbox } from \"@/components/ui/Checkbox\";\nimport { Input } from \"@/components/ui/Input\";\nimport { IconButton } from \"@/components/ui/Button\";\nimport { Badge } from \"@/components/ui/Badge\";\nimport { cn } from \"@/lib/cn\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ntype Tag = \"work\" | \"personal\" | \"urgent\";\n\ninterface Task {\n  id: string;\n  text: string;\n  done: boolean;\n  tag: Tag;\n}\n\nconst SEED = [\n  { id: \"seed-1\", textKey: \"todoList9Task1\", done: false, tag: \"work\" },\n  { id: \"seed-2\", textKey: \"todoList9Task2\", done: false, tag: \"urgent\" },\n  { id: \"seed-3\", textKey: \"todoList9Task3\", done: true, tag: \"personal\" },\n  { id: \"seed-4\", textKey: \"todoList9Task4\", done: false, tag: \"work\" },\n  { id: \"seed-5\", textKey: \"todoList9Task5\", done: false, tag: \"personal\" },\n  { id: \"seed-6\", textKey: \"todoList9Task6\", done: true, tag: \"urgent\" },\n] as const;\n\nconst TAGS: Tag[] = [\"work\", \"personal\", \"urgent\"];\n\nexport function TagFilterSearchTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const TAG_LABEL: Record<Tag, string> = {\n    work: d.todoList9TagWork,\n    personal: d.todoList9TagPersonal,\n    urgent: d.todoList9TagUrgent,\n  };\n  const [tasks, setTasks] = useState<Task[]>(() =>\n    SEED.map((seed) => ({ id: seed.id, text: d[seed.textKey], done: seed.done, tag: seed.tag })),\n  );\n  const [query, setQuery] = useState(\"\");\n  const [activeTag, setActiveTag] = useState<Tag | \"all\">(\"all\");\n\n  function toggleTask(id: string) {\n    setTasks((prev) =>\n      prev.map((task) => (task.id === id ? { ...task, done: !task.done } : task)),\n    );\n  }\n\n  function deleteTask(id: string) {\n    setTasks((prev) => prev.filter((task) => task.id !== id));\n  }\n\n  const filtered = useMemo(() => {\n    const q = query.trim().toLowerCase();\n    return tasks.filter((task) => {\n      const matchesTag = activeTag === \"all\" || task.tag === activeTag;\n      const matchesQuery = !q || task.text.toLowerCase().includes(q);\n      return matchesTag && matchesQuery;\n    });\n  }, [tasks, query, activeTag]);\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <CardTitle>{d.todoList9Heading}</CardTitle>\n            <CardDescription>{d.todoList9Subheading}</CardDescription>\n          </CardHeader>\n          <CardContent className=\"flex flex-col gap-4\">\n            <Input\n              type=\"search\"\n              value={query}\n              onChange={(event) => setQuery(event.target.value)}\n              placeholder={d.todoList9SearchPlaceholder}\n              aria-label={d.todoList9SearchAria}\n              leftIcon={<IconSearch size={16} />}\n            />\n\n            <div className=\"flex flex-wrap gap-1.5\">\n              <button\n                type=\"button\"\n                onClick={() => setActiveTag(\"all\")}\n                className={cn(\n                  \"rounded-full px-3 py-1 text-xs font-medium transition-colors\",\n                  activeTag === \"all\"\n                    ? \"bg-brand text-brand-fg\"\n                    : \"bg-surface text-muted hover:text-fg border-border border\",\n                )}\n              >\n                {d.todoList9TagAll}\n              </button>\n              {TAGS.map((tag) => (\n                <button\n                  key={tag}\n                  type=\"button\"\n                  onClick={() => setActiveTag(tag)}\n                  className={cn(\n                    \"rounded-full px-3 py-1 text-xs font-medium transition-colors\",\n                    activeTag === tag\n                      ? \"bg-brand text-brand-fg\"\n                      : \"bg-surface text-muted hover:text-fg border-border border\",\n                  )}\n                >\n                  {TAG_LABEL[tag]}\n                </button>\n              ))}\n            </div>\n\n            {filtered.length === 0 ? (\n              <p className=\"text-muted py-6 text-center text-sm\">{d.todoList9EmptyResults}</p>\n            ) : (\n              <ul className=\"flex flex-col gap-1\">\n                {filtered.map((task) => (\n                  <li\n                    key={task.id}\n                    className={cn(\n                      \"hover:bg-surface-hover flex items-center justify-between gap-2 rounded-md px-2 py-2\",\n                      task.done && \"opacity-60\",\n                    )}\n                  >\n                    <Checkbox checked={task.done} onChange={() => toggleTask(task.id)} label={task.text} />\n                    <div className=\"flex shrink-0 items-center gap-2\">\n                      <Badge variant=\"soft\" size=\"sm\">\n                        {TAG_LABEL[task.tag]}\n                      </Badge>\n                      <IconButton\n                        icon={<IconTrash size={15} aria-hidden=\"true\" />}\n                        label={d.todoList9DeleteAria.replace(\"{task}\", task.text)}\n                        variant=\"ghost\"\n                        size=\"icon-sm\"\n                        onClick={() => deleteTask(task.id)}\n                      />\n                    </div>\n                  </li>\n                ))}\n              </ul>\n            )}\n          </CardContent>\n        </Card>\n      </div>\n    </section>\n  );\n}\n","todo-list-10":"\"use client\";\n\nimport { useState } from \"react\";\nimport { IconArrowLeft, IconArrowRight, IconTrash } from \"@tabler/icons-react\";\nimport {\n  Card,\n  CardContent,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n} from \"@/components/ui/Card\";\nimport { IconButton } from \"@/components/ui/Button\";\nimport { Badge } from \"@/components/ui/Badge\";\nimport { cn } from \"@/lib/cn\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithTodoListMessages } from \"@/types/pages/todo-list/TodoListMessages-types\";\n\ntype Status = \"todo\" | \"doing\" | \"done\";\n\ninterface Task {\n  id: string;\n  text: string;\n  status: Status;\n}\n\nconst SEED = [\n  { id: \"seed-1\", textKey: \"todoList10Task1\", status: \"todo\" },\n  { id: \"seed-2\", textKey: \"todoList10Task2\", status: \"todo\" },\n  { id: \"seed-3\", textKey: \"todoList10Task3\", status: \"doing\" },\n  { id: \"seed-4\", textKey: \"todoList10Task4\", status: \"doing\" },\n  { id: \"seed-5\", textKey: \"todoList10Task5\", status: \"done\" },\n  { id: \"seed-6\", textKey: \"todoList10Task6\", status: \"done\" },\n] as const;\n\nconst STATUSES: Status[] = [\"todo\", \"doing\", \"done\"];\n\nexport function KanbanBoardTodoList() {\n  const t = useMessages(\"pages\") as unknown as PagesWithTodoListMessages;\n  const d = t.todoList;\n  const COLUMN_LABEL: Record<Status, string> = {\n    todo: d.todoList10ColumnTodo,\n    doing: d.todoList10ColumnDoing,\n    done: d.todoList10ColumnDone,\n  };\n  const [tasks, setTasks] = useState<Task[]>(() =>\n    SEED.map((seed) => ({ id: seed.id, text: d[seed.textKey], status: seed.status })),\n  );\n\n  function deleteTask(id: string) {\n    setTasks((prev) => prev.filter((task) => task.id !== id));\n  }\n\n  function moveTask(id: string, direction: -1 | 1) {\n    setTasks((prev) =>\n      prev.map((task) => {\n        if (task.id !== id) return task;\n        const currentIndex = STATUSES.indexOf(task.status);\n        const nextIndex = currentIndex + direction;\n        if (nextIndex < 0 || nextIndex >= STATUSES.length) return task;\n        return { ...task, status: STATUSES[nextIndex] };\n      }),\n    );\n  }\n\n  return (\n    <section className=\"w-full py-16 lg:py-24\">\n      <div className=\"mx-auto max-w-4xl px-6 lg:px-8\">\n        <Card variant=\"default\">\n          <CardHeader>\n            <CardTitle>{d.todoList10Heading}</CardTitle>\n            <CardDescription>{d.todoList10Subheading}</CardDescription>\n          </CardHeader>\n          <CardContent>\n            <div className=\"grid grid-cols-1 gap-3 sm:grid-cols-3\">\n              {STATUSES.map((status, columnIndex) => {\n                const columnTasks = tasks.filter((task) => task.status === status);\n                return (\n                  <div key={status} className=\"bg-surface flex flex-col gap-2 rounded-lg p-2.5\">\n                    <div className=\"flex items-center justify-between px-1 pt-0.5 pb-1\">\n                      <span className=\"text-fg text-xs font-semibold tracking-wide uppercase\">\n                        {COLUMN_LABEL[status]}\n                      </span>\n                      <Badge variant=\"secondary\" size=\"sm\">\n                        {columnTasks.length}\n                      </Badge>\n                    </div>\n                    {columnTasks.length === 0 ? (\n                      <p className=\"text-muted px-1 py-3 text-center text-xs\">\n                        {d.todoList10EmptyColumn}\n                      </p>\n                    ) : (\n                      <ul className=\"flex flex-col gap-2\">\n                        {columnTasks.map((task) => (\n                          <li\n                            key={task.id}\n                            className={cn(\n                              \"border-border bg-bg flex flex-col gap-2 rounded-md border p-2.5 shadow-xs\",\n                              status === \"done\" && \"opacity-70\",\n                            )}\n                          >\n                            <span\n                              className={cn(\n                                \"text-fg text-sm\",\n                                status === \"done\" && \"line-through\",\n                              )}\n                            >\n                              {task.text}\n                            </span>\n                            <div className=\"flex items-center justify-between\">\n                              <IconButton\n                                icon={<IconArrowLeft size={14} aria-hidden=\"true\" />}\n                                label={d.todoList10MoveBackAria.replace(\"{task}\", task.text)}\n                                variant=\"ghost\"\n                                size=\"icon-xs\"\n                                disabled={columnIndex === 0}\n                                onClick={() => moveTask(task.id, -1)}\n                              />\n                              <IconButton\n                                icon={<IconTrash size={13} aria-hidden=\"true\" />}\n                                label={d.todoList10DeleteAria.replace(\"{task}\", task.text)}\n                                variant=\"ghost\"\n                                size=\"icon-xs\"\n                                onClick={() => deleteTask(task.id)}\n                              />\n                              <IconButton\n                                icon={<IconArrowRight size={14} aria-hidden=\"true\" />}\n                                label={d.todoList10MoveForwardAria.replace(\"{task}\", task.text)}\n                                variant=\"ghost\"\n                                size=\"icon-xs\"\n                                disabled={columnIndex === STATUSES.length - 1}\n                                onClick={() => moveTask(task.id, 1)}\n                              />\n                            </div>\n                          </li>\n                        ))}\n                      </ul>\n                    )}\n                  </div>\n                );\n              })}\n            </div>\n          </CardContent>\n        </Card>\n      </div>\n    </section>\n  );\n}\n"};
export default sources;
