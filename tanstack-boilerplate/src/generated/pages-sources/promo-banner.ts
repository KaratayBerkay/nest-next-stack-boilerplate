// AUTO-GENERATED by scripts/generate-pages-sources.ts — do not edit.
// Regenerate with `pnpm generate-pages-sources`.
const sources: Record<string, string> = {"promo-banner-1":"\"use client\";\n\nimport { useState, type Dispatch, type SetStateAction } from \"react\";\nimport { IconCheck, IconShoppingCart, IconTruck, IconX } from \"@tabler/icons-react\";\nimport { Button, IconButton } from \"@/components/ui/Button\";\nimport { Progress } from \"@/components/ui/Progress\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithPromoBannerMessages } from \"@/types/pages/promo-banner/PromoBannerMessages-types\";\n\nconst FREE_SHIPPING_THRESHOLD = 75;\nconst INITIAL_CART_VALUE = 35;\nconst ADD_AMOUNT = 20;\n\nfunction dismissBanner(setVisible: Dispatch<SetStateAction<boolean>>) {\n  setVisible(false);\n}\n\nexport function FreeShippingProgressPromoBanner() {\n  const t = useMessages(\"pages\") as unknown as PagesWithPromoBannerMessages;\n  const p = t.promoBanner;\n  const [visible, setVisible] = useState(true);\n  const [cartValue, setCartValue] = useState<number>(INITIAL_CART_VALUE);\n\n  if (!visible) return null;\n\n  const remaining = Math.max(0, FREE_SHIPPING_THRESHOLD - cartValue);\n  const percent = Math.round((cartValue / FREE_SHIPPING_THRESHOLD) * 100);\n  const unlocked = remaining <= 0;\n\n  return (\n    <section className=\"border-border bg-surface relative flex h-[600px] w-full flex-col items-stretch overflow-hidden rounded-2xl border\">\n      <div className=\"border-border bg-surface flex w-full flex-col gap-2 border-b px-4 py-3 sm:px-6\">\n        <div className=\"mx-auto flex w-full max-w-3xl flex-wrap items-center justify-between gap-3\">\n          <div className=\"flex min-w-0 flex-1 items-center gap-2\">\n            {unlocked ? (\n              <IconCheck size={18} className=\"text-success shrink-0\" aria-hidden=\"true\" />\n            ) : (\n              <IconTruck size={18} className=\"text-brand shrink-0\" aria-hidden=\"true\" />\n            )}\n            <p className=\"text-fg truncate text-sm font-medium\">\n              {unlocked\n                ? p.promoBanner1Unlocked\n                : p.promoBanner1Message.replace(\"{amount}\", `$${remaining}`)}\n            </p>\n          </div>\n          <div className=\"flex shrink-0 items-center gap-2\">\n            {unlocked ? (\n              <Button\n                type=\"button\"\n                variant=\"ghost\"\n                size=\"xs\"\n                onClick={() => setCartValue(INITIAL_CART_VALUE)}\n              >\n                {p.promoBanner1ResetButton}\n              </Button>\n            ) : (\n              <Button\n                type=\"button\"\n                variant=\"secondary\"\n                size=\"xs\"\n                leftIcon={<IconShoppingCart size={14} />}\n                onClick={() =>\n                  setCartValue((value) => Math.min(FREE_SHIPPING_THRESHOLD, value + ADD_AMOUNT))\n                }\n              >\n                {p.promoBanner1AddButton}\n              </Button>\n            )}\n            <IconButton\n              icon={<IconX size={16} />}\n              label={p.promoBannerCloseAria}\n              variant=\"ghost\"\n              size=\"icon-sm\"\n              onClick={() => dismissBanner(setVisible)}\n            />\n          </div>\n        </div>\n        <div className=\"mx-auto w-full max-w-3xl\">\n          <Progress value={percent} size=\"sm\" />\n        </div>\n      </div>\n    </section>\n  );\n}\n","promo-banner-2":"\"use client\";\n\nimport { useState, type Dispatch, type SetStateAction } from \"react\";\nimport { IconArrowRight, IconX } from \"@tabler/icons-react\";\nimport { IconButton } from \"@/components/ui/Button\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithPromoBannerMessages } from \"@/types/pages/promo-banner/PromoBannerMessages-types\";\n\nconst SHOP_URL = \"https://example.com\" as const;\n\nfunction dismissBanner(setVisible: Dispatch<SetStateAction<boolean>>) {\n  setVisible(false);\n}\n\nexport function TwoToneSplitShopNowPromoBanner() {\n  const t = useMessages(\"pages\") as unknown as PagesWithPromoBannerMessages;\n  const p = t.promoBanner;\n  const [visible, setVisible] = useState(true);\n\n  if (!visible) return null;\n\n  return (\n    <section className=\"border-border bg-surface relative flex h-[600px] w-full flex-col items-stretch overflow-hidden rounded-2xl border\">\n      <div className=\"flex w-full flex-col sm:flex-row\">\n        <div className=\"bg-brand text-brand-fg flex shrink-0 flex-col items-center justify-center gap-0.5 px-6 py-4 sm:w-56\">\n          <span className=\"text-brand-fg/80 text-[11px] font-semibold tracking-widest uppercase\">\n            {p.promoBanner2Eyebrow}\n          </span>\n          <span className=\"text-2xl font-bold tracking-tight\">{p.promoBanner2Percent}</span>\n        </div>\n        <div className=\"bg-muted/15 flex flex-1 flex-wrap items-center justify-between gap-3 px-6 py-4\">\n          <p className=\"text-fg max-w-md text-sm\">{p.promoBanner2Description}</p>\n          <div className=\"flex shrink-0 items-center gap-3\">\n            <a\n              href={SHOP_URL}\n              target=\"_blank\"\n              rel=\"noopener noreferrer\"\n              className=\"text-fg group inline-flex items-center gap-1 text-sm font-semibold\"\n            >\n              {p.promoBanner2ShopNow}\n              <IconArrowRight size={16} className=\"transition-transform group-hover:translate-x-0.5\" />\n            </a>\n            <IconButton\n              icon={<IconX size={16} />}\n              label={p.promoBannerCloseAria}\n              variant=\"ghost\"\n              size=\"icon-sm\"\n              onClick={() => dismissBanner(setVisible)}\n            />\n          </div>\n        </div>\n      </div>\n    </section>\n  );\n}\n","promo-banner-3":"\"use client\";\n\nimport { useEffect, useState, type Dispatch, type SetStateAction } from \"react\";\nimport { IconClock, IconX } from \"@tabler/icons-react\";\nimport { Button, IconButton } from \"@/components/ui/Button\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithPromoBannerMessages } from \"@/types/pages/promo-banner/PromoBannerMessages-types\";\n\n// Demo-only cutoff: ~6h42m out from whenever this block mounts. Computed\n// inside the effect (client-only) so it never leaks into the initial render\n// output — see the useEffect below.\nconst CUTOFF_OFFSET_SECONDS = 6 * 3600 + 42 * 60;\n\nfunction dismissBanner(setVisible: Dispatch<SetStateAction<boolean>>) {\n  setVisible(false);\n}\n\nfunction formatClock(secondsLeft: number | null): string {\n  if (secondsLeft === null) return \"--:--:--\";\n  const hours = Math.floor(secondsLeft / 3600);\n  const minutes = Math.floor((secondsLeft % 3600) / 60);\n  const seconds = secondsLeft % 60;\n  return [hours, minutes, seconds]\n    .map((unit) => String(unit).padStart(2, \"0\"))\n    .join(\":\");\n}\n\nexport function DeliveryCutoffCountdownPromoBanner() {\n  const t = useMessages(\"pages\") as unknown as PagesWithPromoBannerMessages;\n  const p = t.promoBanner;\n  const [visible, setVisible] = useState(true);\n  const [secondsLeft, setSecondsLeft] = useState<number | null>(null);\n\n  useEffect(() => {\n    const target = Date.now() + CUTOFF_OFFSET_SECONDS * 1000;\n    const tick = () => {\n      setSecondsLeft(Math.max(0, Math.round((target - Date.now()) / 1000)));\n    };\n    const initial = setTimeout(tick, 0);\n    const id = setInterval(tick, 1000);\n    return () => {\n      clearTimeout(initial);\n      clearInterval(id);\n    };\n  }, []);\n\n  if (!visible) return null;\n\n  return (\n    <section className=\"border-border bg-surface relative flex h-[600px] w-full flex-col items-stretch overflow-hidden rounded-2xl border\">\n      <div className=\"border-border bg-surface flex w-full flex-wrap items-center justify-center gap-2 border-b px-4 py-3 sm:gap-3 sm:px-6\">\n        <IconClock size={16} className=\"text-brand hidden shrink-0 sm:block\" aria-hidden=\"true\" />\n        <p\n          className=\"flex flex-wrap items-baseline justify-center gap-x-1.5 gap-y-0.5 text-center text-sm\"\n          suppressHydrationWarning\n        >\n          <span className=\"text-muted\">{p.promoBanner3Label}</span>\n          <span\n            className=\"text-fg font-mono text-base font-semibold tabular-nums\"\n            aria-live=\"polite\"\n            aria-label={p.promoBanner3TimerAria}\n          >\n            {formatClock(secondsLeft)}\n          </span>\n          <span className=\"text-muted\">{p.promoBanner3Suffix}</span>\n        </p>\n        <Button type=\"button\" variant=\"primary\" size=\"xs\" className=\"shrink-0\">\n          {p.promoBanner3Button}\n        </Button>\n        <IconButton\n          icon={<IconX size={16} />}\n          label={p.promoBannerCloseAria}\n          variant=\"ghost\"\n          size=\"icon-sm\"\n          className=\"shrink-0\"\n          onClick={() => dismissBanner(setVisible)}\n        />\n      </div>\n    </section>\n  );\n}\n","promo-banner-4":"\"use client\";\n\nimport { Fragment } from \"react\";\nimport type { IconProps } from \"@tabler/icons-react\";\nimport { IconHeadset, IconLock, IconRotate, IconStarFilled } from \"@tabler/icons-react\";\nimport { Separator } from \"@/components/ui/Separator\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithPromoBannerMessages } from \"@/types/pages/promo-banner/PromoBannerMessages-types\";\n\ninterface TrustItem {\n  id: string;\n  icon: React.ComponentType<IconProps>;\n  labelKey: string;\n}\n\nconst ITEMS: TrustItem[] = [\n  { id: \"secure\", icon: IconLock, labelKey: \"promoBanner4Item1\" },\n  { id: \"returns\", icon: IconRotate, labelKey: \"promoBanner4Item2\" },\n  { id: \"rating\", icon: IconStarFilled, labelKey: \"promoBanner4Item3\" },\n  { id: \"support\", icon: IconHeadset, labelKey: \"promoBanner4Item4\" },\n];\n\nexport function TrustSignalMinimalPromoBanner() {\n  const t = useMessages(\"pages\") as unknown as PagesWithPromoBannerMessages;\n  const p = t.promoBanner;\n\n  return (\n    <section className=\"border-border bg-surface relative flex h-[600px] w-full flex-col items-stretch overflow-hidden rounded-2xl border\">\n      <div className=\"bg-muted/10 w-full py-3\">\n        <div className=\"mx-auto flex w-full max-w-4xl flex-wrap items-center justify-center gap-x-6 gap-y-2 px-4 sm:px-6\">\n          {ITEMS.map((item, index) => (\n            <Fragment key={item.id}>\n              {index > 0 && (\n                <Separator orientation=\"vertical\" className=\"hidden h-4 sm:block\" />\n              )}\n              <div className=\"flex items-center gap-1.5\">\n                <item.icon size={15} className=\"text-muted shrink-0\" aria-hidden=\"true\" />\n                <span className=\"text-fg text-xs font-medium sm:text-sm\">{p[item.labelKey]}</span>\n              </div>\n            </Fragment>\n          ))}\n        </div>\n      </div>\n    </section>\n  );\n}\n","promo-banner-5":"\"use client\";\n\nimport { useEffect, useState, type Dispatch, type SetStateAction } from \"react\";\nimport { IconBolt, IconX } from \"@tabler/icons-react\";\nimport { Button, IconButton } from \"@/components/ui/Button\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithPromoBannerMessages } from \"@/types/pages/promo-banner/PromoBannerMessages-types\";\n\n// Demo-only sale end: ~5h45m out from whenever this block mounts. Computed\n// inside the effect (client-only) so it never leaks into the initial render\n// output — see the useEffect below.\nconst SALE_OFFSET_SECONDS = 5 * 3600 + 45 * 60;\nconst SHOP_URL = \"https://example.com\" as const;\n\ninterface Remaining {\n  hours: number;\n  minutes: number;\n  seconds: number;\n}\n\nconst UNITS = [\n  { id: \"hours\", labelKey: \"promoBanner5UnitHours\" },\n  { id: \"minutes\", labelKey: \"promoBanner5UnitMinutes\" },\n  { id: \"seconds\", labelKey: \"promoBanner5UnitSeconds\" },\n] as const;\n\nfunction dismissBanner(setVisible: Dispatch<SetStateAction<boolean>>) {\n  setVisible(false);\n}\n\nexport function FlashSaleGradientCountdownPromoBanner() {\n  const t = useMessages(\"pages\") as unknown as PagesWithPromoBannerMessages;\n  const p = t.promoBanner;\n  const [visible, setVisible] = useState(true);\n  const [remaining, setRemaining] = useState<Remaining | null>(null);\n\n  useEffect(() => {\n    const target = Date.now() + SALE_OFFSET_SECONDS * 1000;\n    const tick = () => {\n      const diff = Math.max(0, target - Date.now());\n      setRemaining({\n        hours: Math.floor(diff / 3_600_000),\n        minutes: Math.floor((diff / 60_000) % 60),\n        seconds: Math.floor((diff / 1_000) % 60),\n      });\n    };\n    const initial = setTimeout(tick, 0);\n    const id = setInterval(tick, 1000);\n    return () => {\n      clearTimeout(initial);\n      clearInterval(id);\n    };\n  }, []);\n\n  if (!visible) return null;\n\n  const valueFor = (unitId: (typeof UNITS)[number][\"id\"]) =>\n    remaining ? String(remaining[unitId]).padStart(2, \"0\") : \"--\";\n\n  return (\n    <section className=\"border-border bg-surface relative flex h-[600px] w-full flex-col items-stretch overflow-hidden rounded-2xl border\">\n      <div className=\"bg-gradient-to-br from-brand to-info text-brand-fg relative w-full\">\n        <IconButton\n          icon={<IconX size={16} />}\n          label={p.promoBannerCloseAria}\n          size=\"icon-sm\"\n          className=\"text-brand-fg hover:bg-brand-fg/10 absolute top-3 right-3\"\n          onClick={() => dismissBanner(setVisible)}\n        />\n        <div className=\"mx-auto flex w-full max-w-3xl flex-col items-center gap-4 px-6 py-10 text-center lg:py-14\">\n          <span className=\"bg-brand-fg/15 inline-flex items-center gap-1.5 rounded-full px-3 py-1 text-xs font-semibold tracking-widest uppercase\">\n            <IconBolt size={13} aria-hidden=\"true\" />\n            {p.promoBanner5Eyebrow}\n          </span>\n          <h2 className=\"text-2xl font-bold tracking-tight sm:text-3xl\">{p.promoBanner5Heading}</h2>\n          <p className=\"text-brand-fg/85 max-w-md text-sm\">{p.promoBanner5Body}</p>\n          <div className=\"flex items-center gap-2 sm:gap-3\" suppressHydrationWarning>\n            {UNITS.map((unit) => (\n              <div\n                key={unit.id}\n                className=\"bg-brand-fg/15 flex w-14 flex-col items-center gap-0.5 rounded-lg py-2 sm:w-16\"\n              >\n                <span className=\"text-lg font-bold tabular-nums sm:text-xl\">{valueFor(unit.id)}</span>\n                <span className=\"text-brand-fg/70 text-[10px] tracking-wide uppercase\">\n                  {p[unit.labelKey]}\n                </span>\n              </div>\n            ))}\n          </div>\n          <Button variant=\"secondary\" size=\"sm\" className=\"mt-1\" asChild>\n            <a href={SHOP_URL} target=\"_blank\" rel=\"noopener noreferrer\">\n              {p.promoBanner5Button}\n            </a>\n          </Button>\n        </div>\n      </div>\n    </section>\n  );\n}\n","promo-banner-6":"\"use client\";\n\nimport { IconGift } from \"@tabler/icons-react\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithPromoBannerMessages } from \"@/types/pages/promo-banner/PromoBannerMessages-types\";\n\n// Demo-only days-left value for the fixed badge — a whole-day countdown\n// doesn't need per-second ticking like the hour/minute/second timers do.\nconst DAYS_LEFT = 12;\n\nconst MARQUEE_CSS = `\n@keyframes promo-banner-marquee-scroll {\n  from { transform: translateX(0); }\n  to { transform: translateX(-50%); }\n}\n.animate-promo-banner-marquee {\n  animation: promo-banner-marquee-scroll 22s linear infinite;\n}\n@media (prefers-reduced-motion: reduce) {\n  .animate-promo-banner-marquee {\n    animation: none;\n  }\n}\n`;\n\ninterface MarqueeMessage {\n  id: string;\n  labelKey: string;\n}\n\nconst MESSAGES: MarqueeMessage[] = [\n  { id: \"sale\", labelKey: \"promoBanner6Message1\" },\n  { id: \"wrap\", labelKey: \"promoBanner6Message2\" },\n  { id: \"returns\", labelKey: \"promoBanner6Message3\" },\n];\n\nexport function HolidayMarqueeCountdownPromoBanner() {\n  const t = useMessages(\"pages\") as unknown as PagesWithPromoBannerMessages;\n  const p = t.promoBanner;\n  const sequence = [...MESSAGES, ...MESSAGES];\n\n  return (\n    <section className=\"border-border bg-surface relative flex h-[600px] w-full flex-col items-stretch overflow-hidden rounded-2xl border\">\n      <style>{MARQUEE_CSS}</style>\n      <div className=\"bg-brand text-brand-fg flex w-full items-stretch\">\n        <div className=\"flex shrink-0 items-center px-4 py-3\">\n          <span className=\"bg-brand-fg/15 inline-flex items-center gap-1.5 rounded-full px-3 py-1 text-xs font-semibold whitespace-nowrap\">\n            <IconGift size={13} aria-hidden=\"true\" />\n            {p.promoBanner6DaysLeft.replace(\"{count}\", String(DAYS_LEFT))}\n          </span>\n        </div>\n        <div className=\"relative flex-1 overflow-hidden py-3\">\n          <div className=\"from-brand pointer-events-none absolute inset-y-0 left-0 z-10 w-10 bg-gradient-to-r to-transparent\" />\n          <div className=\"from-brand pointer-events-none absolute inset-y-0 right-0 z-10 w-10 bg-gradient-to-l to-transparent\" />\n          <div className=\"animate-promo-banner-marquee flex w-max items-center gap-8 whitespace-nowrap\">\n            {sequence.map((message, index) => (\n              <span key={`${message.id}-${index}`} className=\"text-sm font-medium\">\n                {p[message.labelKey]}\n              </span>\n            ))}\n          </div>\n        </div>\n      </div>\n    </section>\n  );\n}\n","promo-banner-7":"\"use client\";\n\nimport { useState, type Dispatch, type SetStateAction } from \"react\";\nimport Image from \"next/image\";\nimport { IconFlame, IconShoppingCart, IconX } from \"@tabler/icons-react\";\nimport { Badge } from \"@/components/ui/Badge\";\nimport { Button, IconButton } from \"@/components/ui/Button\";\nimport { Progress } from \"@/components/ui/Progress\";\nimport { useMessages } from \"@/lib/i18n/MessagesProvider\";\nimport type { PagesWithPromoBannerMessages } from \"@/types/pages/promo-banner/PromoBannerMessages-types\";\nimport { placeholderImage } from \"@/views/pages/_shared/placeholder-image\";\n\nconst STOCK_LEFT = 6;\nconst STOCK_TOTAL = 40;\n\nfunction dismissBanner(setVisible: Dispatch<SetStateAction<boolean>>) {\n  setVisible(false);\n}\n\nexport function StickyBottomStockBarPromoBanner() {\n  const t = useMessages(\"pages\") as unknown as PagesWithPromoBannerMessages;\n  const p = t.promoBanner;\n  const [visible, setVisible] = useState(true);\n\n  if (!visible) return null;\n\n  const stockPercent = Math.round((STOCK_LEFT / STOCK_TOTAL) * 100);\n\n  return (\n    <section className=\"border-border bg-surface relative flex h-[600px] w-full flex-col items-stretch justify-end overflow-hidden rounded-2xl border\">\n      <div className=\"border-border bg-bg animate-fade-in-up w-full border-t px-4 py-4 motion-reduce:animate-none sm:px-6\">\n        <div className=\"mx-auto flex w-full max-w-3xl flex-wrap items-center gap-4\">\n          <div className=\"bg-surface relative size-14 shrink-0 overflow-hidden rounded-lg\">\n            <Image\n              src={placeholderImage(\"promo-banner-7\", \"1x1\")}\n              alt={p.promoBanner7ImageAlt}\n              fill\n              sizes=\"56px\"\n              className=\"object-cover\"\n            />\n          </div>\n          <div className=\"flex min-w-0 flex-1 flex-col gap-1.5\">\n            <div className=\"flex flex-wrap items-center gap-2\">\n              <Badge variant=\"error\" size=\"sm\" pill className=\"gap-1\">\n                <IconFlame size={12} aria-hidden=\"true\" />\n                {p.promoBanner7Badge}\n              </Badge>\n              <p className=\"text-fg truncate text-sm font-medium\">{p.promoBanner7ProductName}</p>\n            </div>\n            <div className=\"flex items-center gap-2\">\n              <Progress value={stockPercent} size=\"sm\" className=\"max-w-32\" />\n              <span className=\"text-muted shrink-0 text-xs\">\n                {p.promoBanner7StockLabel.replace(\"{count}\", String(STOCK_LEFT))}\n              </span>\n            </div>\n          </div>\n          <div className=\"flex shrink-0 items-center gap-3\">\n            <div className=\"hidden flex-col items-end sm:flex\">\n              <span className=\"text-fg text-base font-semibold\">{p.promoBanner7SalePrice}</span>\n              <span className=\"text-muted text-xs line-through\">{p.promoBanner7OriginalPrice}</span>\n            </div>\n            <Button type=\"button\" variant=\"primary\" size=\"sm\" rightIcon={<IconShoppingCart size={14} />}>\n              {p.promoBanner7Cta}\n            </Button>\n            <IconButton\n              icon={<IconX size={16} />}\n              label={p.promoBannerCloseAria}\n              variant=\"ghost\"\n              size=\"icon-sm\"\n              onClick={() => dismissBanner(setVisible)}\n            />\n          </div>\n        </div>\n      </div>\n    </section>\n  );\n}\n"};
export default sources;
