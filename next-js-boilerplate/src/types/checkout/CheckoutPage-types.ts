export interface CheckoutPageProps {
  params: Promise<{ lang: string; tier: string }>;
  className?: string;
}

export interface CheckoutContentPageProps {
  params: Promise<{ lang: string; tier: string }>;
  className?: string;
}
