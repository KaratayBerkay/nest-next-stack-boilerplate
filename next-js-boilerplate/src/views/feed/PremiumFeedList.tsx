// The premium feed is the shared FeedList with `currentUserId` supplied by
// PremiumPageView (own posts get a crown badge) — it used to be a full copy
// of FreeFeedList differing only in that badge.
export { FeedList } from "@/views/feed/FreeFeedList";
