// A string DI token for an OPTIONAL dependency — used to demonstrate @Inject + @Optional and a
// `useValue` custom provider overriding the fallback. (Non-class tokens need an explicit @Inject.)
export const GREETING = 'GREETING';
