# name: Build iOS IPA
#
# DISABLED: Requires an Apple Developer account ($99/year) to sign the app
# for device installation. Re-enable by uncommenting below and adding these
# GitHub Secrets (repo → Settings → Secrets → Actions):
#
#   BUILD_CERTIFICATE_BASE64   — base64-encoded .p12 distribution certificate
#   P12_PASSWORD               — password for the .p12 file
#   BUILD_PROVISION_PROFILE_BASE64 — base64-encoded .mobileprovision profile
#   KEYCHAIN_PASSWORD          — any random string (e.g. openssl rand -hex 16)
#   APPLE_TEAM_ID              — 10-character team ID from Apple Developer portal
#
# Also update ios/ExportOptions.plist with your team ID and profile name.
#
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
#
# jobs:
#   build-ios:
#     name: Build & Sign iOS IPA
#     runs-on: macos-14
#     timeout-minutes: 45
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Select Xcode
#         uses: maxim-lobanov/setup-xcode@v1
#         with:
#           xcode-version: "16.4"
#
#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           flutter-version: "3.44.2"
#           channel: stable
#           cache: true
#           pub-cache: true
#
#       - name: Get dependencies
#         run: flutter pub get
#
#       - name: Cache CocoaPods
#         uses: actions/cache@v4
#         with:
#           path: |
#             flutter-boilerplate/ios/Pods
#             ~/Library/Caches/CocoaPods
#             ~/.cocoapods/repos
#           key: ${{ runner.os }}-pods-${{ hashFiles('flutter-boilerplate/ios/Podfile.lock') }}
#           restore-keys: |
#             ${{ runner.os }}-pods-
#
#       - name: Install CocoaPods
#         working-directory: flutter-boilerplate/ios
#         run: pod install --repo-update
#
#       - name: Install Apple Certificate
#         env:
#           BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
#           P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
#           BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }}
#           KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
#         run: |
#           CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
#           PP_PATH=$RUNNER_TEMP/build_pp.mobileprovision
#           KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
#
#           echo -n "$BUILD_CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH
#           echo -n "$BUILD_PROVISION_PROFILE_BASE64" | base64 --decode -o $PP_PATH
#
#           security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
#           security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
#           security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
#
#           security import $CERTIFICATE_PATH \
#             -P "$P12_PASSWORD" \
#             -A -t cert -f pkcs12 \
#             -k $KEYCHAIN_PATH
#
#           security set-key-partition-list \
#             -S apple-tool:,apple: \
#             -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
#
#           security list-keychain -d user -s $KEYCHAIN_PATH
#
#           mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
#           cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles/
#
#       - name: Build IPA
#         working-directory: flutter-boilerplate
#         run: |
#           flutter build ipa --release \
#             --export-options-plist=ios/ExportOptions.plist \
#             --dart-define-from-file=.env.example \
#             --build-number=${{ github.run_number }} \
#             --build-name=1.0.${{ github.run_number }}
#
#       - name: Upload IPA Artifact
#         if: success()
#         uses: actions/upload-artifact@v4
#         with:
#           name: ios-build-${{ github.run_number }}
#           path: flutter-boilerplate/build/ios/ipa/*.ipa
#           retention-days: 30
#           if-no-files-found: error
#
#       - name: Cleanup
#         if: always()
#         run: |
#           security delete-keychain $RUNNER_TEMP/app-signing.keychain-db 2>/dev/null || true
#           rm -f $RUNNER_TEMP/build_certificate.p12 2>/dev/null || true
#           rm -f $RUNNER_TEMP/build_pp.mobileprovision 2>/dev/null || true
