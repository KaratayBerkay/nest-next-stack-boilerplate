# NestJS Feature Checklist

> Feature-by-feature progress against the docs.nestjs.com table of contents.
> **✅** = implemented, **🧪** = verified by a proof test, **🔵** = in progress, **⬜** = not started.

| # | Feature | Status | Proof |
|---|---|---|---|
| 32 | Logging — Pino structured JSON app logger + correlation + ELK transport | ✅🧪 | `test/logging.e2e-spec.ts`, `src/logging/logging.config.spec.ts` |
