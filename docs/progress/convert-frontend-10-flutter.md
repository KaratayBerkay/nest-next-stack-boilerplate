# convert-frontend-10-flutter — Messaging features, upload pipeline, settings overhaul, and a structural auth-loop fix

**Date:** 2026-07-31 · **Status:** ✅ **All implementation boxes T1–T73 checked — Stages A–I complete and unit-verified; only live round-trip verification remains (deferred to §5's live gate: T6 MFA round-trip, Stage C infra checks, Stage D upload/emoji round-trips, Stages F/G live Stripe/comment round-trips).** Stage G (T58–T64, feed comment bugs) and Stage H (T65–T69, settings/general UI polish) implemented + unit-verified this session; Stage I completed with T72 deleted per Berkay's sign-off (T70/T71/T73 folded into earlier stages). Five parallel deep-dive investigations (full file:line evidence, across `nest-js-boilerplate` + `next-js-boilerplate` + `flutter-boilerplate`) covered all 8 areas Berkay reported. This doc is the design + task breakdown for review before implementation starts, per this project's established phased-roadmap-workflow convention ("plan phase N" = write the tracker only, no code in the same pass). convert-frontend-9 closed clean on 2026-07-28 (all 3 gates green for the first time). Two commits landed on `main` *after* that close — `3aff834d`, `c0982fa0` — attempting a narrow auth-loop fix that was never verified by any doc; folded into Stage A below since it's directly relevant to item 9.

---

## 0. How to read this doc

Berkay's 8 reports map to findings and stages like this:

| # | Report (paraphrased) | Severity | Findings | Stage |
|---|---|---|---|---|
| 1 | Messages: need file upload | Feature (greenfield) | F7 | D |
| 2 | Messages + chat-room: need an emoji button | Feature (greenfield) | F8 | D |
| 3 | Messages: friend list doesn't live-update when a request is accepted; must not hot-switch the open chat | High | F1, F2 | B |
| 4 | Chat-room: doesn't scroll to bottom on new message | Medium | F3 | B |
| 5 | Settings/Account: photo upload doesn't show — S3 issue? | High | F5, F6 | C |
| 6 | Settings/Privacy: chat nickname saved, chat-room still shows real name | High | F4 | B |
| 7 | Settings/Sessions: "No active sessions found"; want device/IP + "which session is mine" | High | F9–F12 | E |
| 8 | Settings/Billing: risk write-up; Invoices its own tab; Plan Benefits as a default-collapsed accordion | High (risk) / Medium (UI) | F13–F19 | F |
| 9 | Settings/General: remove theme button; info boxes look rushed; **auth keeps cycling `/auth/login` ⇄ `/v1/en/feed`** | Low (UI) / **Critical (loop)** | F20–F25 | H / **A** |
| 10 | Feed: new comment never shows (even after reload); edited comment doesn't refresh | High | F26–F31 | G |

**If you only read one section, read §4.1 (Stage A)** — the auth loop is app-wide and the highest-confidence structural finding in this doc.

**Two things this pass could not verify:** the two screenshots Berkay attached ([Image #1] theme button, [Image #2] info boxes) never reached the assistant — §2.6/§4.8's UI recommendations come from reading the component code directly, not the screenshots; sanity-check against what was actually shown before implementing. Second, every finding below is explicit about **confirmed by reading code** vs **plausible, needs live verification** — this investigation ran with no docker/build/live-repro access by design (Berkay's ask was "investigate," not "reproduce live"); treat the "needs live verification" tags as real, not decorative.

---

## 1. Executive summary — the ten findings that matter most

1. **The `/auth/login` ⇄ `/v1/en/feed` loop is fully deterministic, not a race.** The automatic 401-handler (`onAuthLogout` in `next-js-boilerplate/src/features/auth/hooks/useAuth.tsx:113-130`) clears only in-memory React state and hard-navigates — it **never clears the BFF cookies**. The stale `session_user` cookie then convinces the next page load's server-side check (`getSessionUser()`) the user is still logged in, bouncing back to `/v1/en/feed`, which fails the same backend check again. (F21, Stage A)
2. **This app's "token refresh" doesn't refresh anything.** `/api/auth/token` (`next-js-boilerplate/src/app/api/auth/token/route.ts`) just echoes whatever's already in the cookie jar. The backend has a fully correct `refresh` GraphQL mutation (`auth-session.service.ts:52-138`) that the web frontend **never calls anywhere**. Once a session goes stale there is no repair path except a fresh login. (F22, Stage A)
3. **Concrete, code-confirmed triggers that desync a "valid-looking" cookie from the backend's opinion**: changing your subscription tier immediately rewrites every live session's RBAC token backend-side with no matching cookie update (F23); a UTC-midnight token-derivation cutover (F23); and a Redis refresh-index key whose TTL never slides with activity, so refresh capability would silently die 15 minutes after login regardless of usage even if a real refresh flow existed (F11, Stage E — fix together with Stage A).
4. **Two commits already landed 3 days ago (2026-07-28) attempting a narrower, MFA-specific fix** for a related session-key-mismatch bug (`3aff834d`, `c0982fa0`) — after convert-frontend-9's doc had already closed clean, so this was never verified by that doc's rounds. Backend unit tests for it pass (20/20, confirmed live this session), but it doesn't touch the general-case loop in #1/#2. (F25, Stage A)
5. **Billing has a critical financial bug.** Upgrading between two paid tiers (e.g. BASIC→MEDIUM) never cancels the old Stripe subscription before creating a new one, and overwrites the only stored reference to it (`billing.service.ts:56-169`) — the customer is billed for both, indefinitely, with no in-app way to stop the first one. (F13, Stage F)
6. **Billing invoice amounts are wrong by roughly 100x.** The Stripe webhook stores `amount_paid` (cents) divided by 100 as if that were the final amount (`stripe-webhook.controller.ts:97`); the frontend then divides by 100 again assuming cents (`InvoiceTable.tsx:98`). A $19.99 charge displays as **$0.20**. Zero test coverage exists for the webhook controller. (F17, Stage F)
7. **The avatar upload bug has two independent, stacked root causes.** `MINIO_PUBLIC_URL` is missing from the backend's Vault secret, so the URL-builder falls back to `http://minio:9000` — a Docker-internal hostname no browser can resolve (`minio.service.ts:22-25`). *Even after fixing the hostname*, `UpdateProfileInput.avatarUrl`'s `@IsUrl()` validation rejects any non-FQDN URL by default — confirmed directly against the installed `validator@13.15.35` package, `http://localhost:9000/...` fails too. The real fix needs an actual public domain wired through Vault, not a one-line hostname swap. (F5, F6, Stage C)
8. **Feed comments have two independent bugs, not one.** Replies are structurally excluded from the feed's post query — an unconditional `parentId: null` Prisma filter (`post.service.ts:205-207`) that never selects `parentId` at all. This is permanent, not a timing issue. Separately, a 3-layer, never-invalidated cache stack (browser `Cache-Control`, Next.js server fetch-cache, Redis) sits in front of the same read path specifically on the feed page, explaining why even a hard reload doesn't help. (F26, F27, Stage G)
9. **The friend-request live-update bug is a scoping mismatch, already half-fixed by existing code.** The event uses page-scoped delivery (`emitToPage`, targeting a `"friend-request"` page claim) but the Messages page claims `"messages"` — the frame is silently dropped for anyone there. The frontend's invalidation logic (`renew-dispatch.ts:85-91`) is already correct and just needs the event to arrive; switching to the same non-page-scoped delivery already used for notifications and new messages fixes it. (F1, Stage B)
10. **The chat-nickname feature doesn't exist end-to-end, on either platform** — this is a build item, not a wiring bug. The privacy settings page's save handler builds a payload and never sends it to any API, on **both** Web and Flutter. Even if it did, no nickname field exists anywhere in the Prisma schema or the WS presence payload. (F4, Stage B)

**A cross-cutting pattern worth naming once, since it independently recurs in three unrelated places:** an authenticated, per-user, mutable GraphQL read gets proxied through a BFF route that defaults to `next: {revalidate: 60}` unless the route explicitly opts out with `noCache: true` (`lib/backend.ts:244-273`) — and only `auth/login`'s route does, anywhere in the app. This same omission independently produces stale reads in feed comments (F27), the sessions list (F11), and — per Agent C's report — `api-keys/route.ts` has the identical gap. See §3's open decision: patch each call site, or flip the shared helper's default.

---

## 2. Findings (full detail)

Each finding is tagged **Confirmed** (established by directly reading the current code, no live check needed to trust it) or **Plausible** (a concrete, code-grounded hypothesis that would benefit from a live/runtime check before treating as fully settled).

### 2.1 Messaging & chat-room

**F1 — Friend-request live-update never reaches the Messages page.** Confirmed. Friend send/accept/decline logic lives in `MessagingFriendService`/`MessagingController` (`nest-js-boilerplate/src/messaging/`), not the `friends` module (which is read-only and emits nothing). On accept, `messaging.controller.ts:103-119` calls `this.realtime.emitToPage(userId, 'friend-request', {renew:'Friends', type:'PendingList'})`. `emitToPage` (`realtime.gateway.ts:601-621` → `realtime-page.manager.ts:217-235`) only delivers to sockets whose page-claim key is literally `page:friend-request:{userId}:*`. Per `next-js-boilerplate/src/lib/realtime/route-mapping.ts:10-17`, `/v1/en/messages` claims page `"messages"`, while only `/v1/en/find-friends` claims `"friend-request"` — two different keys, so a user on Messages never receives the frame. The frontend's consumer (`renew-dispatch.ts:85-91`, invalidating `["friends","requests"]` and `["friends","list"]`) is already correct and would work immediately once the frame arrives. Contrast: the notification bell updates because `NotificationService.create` uses `emitToService(userId, 'NOTIFICATION', ...)` — not page-scoped, delivered to every connected socket regardless of current page (every client registers for it unconditionally on connect, `useRealtimeCoordination.ts:89,217`). New-message renews use the same broad pattern (`messaging-dm.service.ts:216-230`). Friends is the one outlier still using page-scoping.
*Secondary factor:* `friendsQueryOptions()` has no `staleTime` override, inheriting the global 5-minute default (`constants/ui.ts:11-14`), so even a window-refocus refetch wouldn't help inside that window — consistent with "only a full reload fixes it."
*Fix direction:* T9-T10.

**F2 — No-hot-switch constraint: currently safe, but a real regression risk once F1 is fixed.** Confirmed safe today. Grepped every `setSelectedUser` call site (3 total) — none is a "refetch → auto-select" pattern; `selectedUser` is isolated `useState` in `useMessagesPage.ts:39`, never derived by indexing into `conversations`/`friends`. This is not a bug to fix, it's a guardrail: whoever implements F1's fix must not derive `selectedUser` from the newly-invalidated list data (e.g. `list.find(...) ?? list[0]`), which would silently reintroduce exactly the regression Berkay called out. *Fix direction:* T11 (no code, a constraint on T9).

**F3 — Chat-room doesn't scroll to bottom: sentinel div wired outside the scrollable container.** Confirmed. Both pages share `next-js-boilerplate/src/hooks/useAutoScroll.ts`, which calls `bottomRef.current?.scrollIntoView(...)` and only works if `bottomRef`'s node lives *inside* the `overflow-y-auto` element as its last child. `ChatMessageList.tsx:21-24,57` (Messages page) does this correctly — sentinel is the last child inside the scrollable div. `ChatRoomMessageList.tsx:17-25` (chat-room) renders the scrollable list with **no sentinel inside it at all**; the sentinel is instead added by the parent, `ChatRoomMainContent.tsx:90-99`, as a **sibling** of `ChatRoomMessageList`, inside an `overflow-hidden` wrapper (line 67) — not an ancestor of the real scrolling element. `scrollIntoView()` on a node with no scrollable ancestor has no effect on the actual list. This also means the `isAtBottom` state (same `IntersectionObserver`, watching this same misplaced sentinel) is unreliable, so the floating "scroll to bottom" button likely also fails to appear correctly — a secondary symptom of the same root cause. Not a missing-dependency bug, not a "never ported" gap — specifically a sibling-vs-child DOM nesting mistake introduced when chat-room was built. **Flutter unaffected** — both `chat_view.dart` and `chat_room_base_view.dart` use a `ScrollController` attached directly to the `ListView.builder`, no sentinel div, no analogous mistake possible (code comments in both files suggest a related issue was already found and fixed there). *Fix direction:* T12.

**F4 — Chat nickname: not a caching bug, the feature doesn't exist end-to-end.** Confirmed. `views/settings/privacy/FreePageView.tsx:17-25`'s `handleSave` builds `{hideProfilePicture, useNickname, nickname}` and **never sends it anywhere** — no `fetch`, no mutation call, just an unconditional "Preferences saved" toast. There's also no initial-value fetch, so the toggle/field always start blank. Even if it did save: no nickname/alias field exists on the `User` Prisma model (searched schema + `profile`/`users` broadly); `UpdateProfileInput` only has `name`/`username`/`bio`/`avatarUrl`/`locale`/`timezone`. The presence layer has no slot either — `ws.userName` is set once at socket-auth via `displayName()` (`common/utils/display-name.ts:1-6`, `name || email || 'Unknown'`), and the `RoomMember` type (`messaging.types.ts:1-5`) constructed at both room-join sites in `messaging-ws.gateway.ts:158-162,260-264` has no nickname field. `ChatRoomSidebar.tsx:99-106` renders `{m.name}` verbatim, with nothing to prefer even if it existed. **Flutter has the identical gap**, faithfully ported: `privacy/page_view.dart:57-59`'s `_save()` is the same discard-the-payload toast, and `chat_room_sidebar.dart:130,142` renders the same nameless `RoomMember` shape. *Fix direction:* T13-T18 (backend+web), T21-T22 (Flutter).

### 2.2 Avatar / S3 upload

**F5 — `MINIO_PUBLIC_URL` missing → every returned avatar URL is a Docker-internal hostname.** Confirmed. Storage is real MinIO (not a mock) — `docker-compose.yml` runs a real `minio/minio` container plus a bucket-provisioning job, and backend credentials in `.vault-envs/backend.env` are genuine dev-default creds, not broken placeholders. But `MINIO_PUBLIC_URL` is referenced in exactly one place, `minio.service.ts:22-25`, and is **absent** from the backend's Vault secret — so the fallback `` `http://${endPoint}:${port}` `` always fires, producing `http://minio:9000/uploads/<uuid>-full.webp` for every upload. No browser or mobile device can resolve `minio` as a hostname. Notably, root `.env`/`next-js-boilerplate/.env.local`/the `frontend` Vault secret all define a plausible real value (`NEXT_PUBLIC_MINIO_PUBLIC_URL=https://app.eys.gen.tr/api`) — but it's read **nowhere** in the frontend (zero matches repo-wide), isn't forwarded as a Next.js build arg in `docker-compose.yml` either, and wouldn't matter regardless since the URL is server-generated by `MinioService` and handed to the client in the API response, never constructed client-side. This looks like an abandoned partial fix — the right value exists in three places, just never landed in the one place (`backend`'s Vault secret) that would use it. **Cannot fully confirm** whether the external openresty reverse proxy actually routes `/api/uploads/*` through to MinIO without live/infra access — the chosen value strongly implies someone intended exactly that.
*Fix direction:* T23.

**F6 — Even a hostname fix would be rejected by validation.** Confirmed, verified directly against the installed library (no server run — a pure library call): `UpdateProfileInput.avatarUrl` (`update-profile.input.ts:34-37`) is `@IsUrl()` with no options, and class-validator's default is `require_tld: true`. Tested against `validator@13.15.35` directly:
```
isURL('http://minio:9000/uploads/x-full.webp')                       -> false
isURL('http://localhost:9000/uploads/x-full.webp')                   -> false  (even localhost fails)
isURL('https://app.eys.gen.tr/api/uploads/x-full.webp')               -> true
```
Nest's global `ValidationPipe` (`main.ts:125-132`, `whitelist/forbidNonWhitelisted/transform: true`) genuinely enforces this — the mutation 400s before `ProfileService.updateProfile()` ever runs, so `avatarUrl` never reaches Postgres. **The persistence layer itself is fine** (confirmed: `profile.service.ts:29-69` correctly writes `avatarUrl` and mirrors it into the Redis session hash) — nothing ever reaches it because of F5+F6 stacked together. Compounding UX problem: the GraphQL error is correctly shaped server-side (`extensions:{exc,msg,key,fields}`) but the frontend's `graphqlErrorBody()` (`lib/backend.ts:220-230`) only surfaces the generic top-level `msg` ("Validation failed"), dropping the field-specific reason — so even the one path that *does* throw visibly gives no hint this is upload/avatar-related. On the client: `Avatar`'s `onError` fallback to initials (`avatar.tsx:60-68`) fires silently with zero console signal for the browser-can't-resolve-hostname case, explaining "nothing appeared to happen" precisely. One dead no-op found in passing: `useProfileActions()` calls `queryClient.invalidateQueries({queryKey:["profile"]})` after upload/update, but nothing anywhere subscribes to that query key (`FreePageView.tsx` fetches via a raw `useEffect`, not react-query) — harmless, but worth cleaning up while in this code.
*Flutter*: shares F5+F6 exactly (same backend endpoints via Dio). Additionally has its **own** separate bug: `_profileProvider` (`page_view.dart:18-21`) is never invalidated after a successful save — even a backend fix wouldn't show the new avatar on that screen without navigating away and back. Flutter also auto-saves on pick (unlike web's explicit-Save flow) and shows an immediate `showToast('Failed: $e')` on error — a real, useful behavioral divergence from web's silent failure, worth keeping.
*Fix direction:* T24-T26 (backend+web), T27 (Flutter).

### 2.3 New features (greenfield feasibility)

**F7 — Message file attachments: fully greenfield, two composers on two different transports.** Confirmed. `Message`/`RoomMessage` Prisma models have only `body String` — no attachment field (`Post`/`Comment` elsewhere use a simple `imageUrl String?` convention worth mirroring rather than a new relational table). `/v1/en/messages` uses `ChatInputBar.tsx`, which **already has a disabled, dead paperclip button** (lines 88-95, no `onClick`) — sending goes over REST (`messaging.controller.ts:178-192`). `/v1/en/chat-room` has an entirely separate composer (`ChatRoomSubComponents.tsx`) with **no attach affordance at all**, sending over WS (`messaging-ws.gateway.ts:191-219`) — so this can't be a single shared fix. The upload module (`upload.controller.ts:25`) is hardcoded to an image-only allowlist and forces every upload through `ImageService.resize()` — no notion of "purpose," so non-image attachments (PDF, docs) can't go through today at all. Interesting tell: Flutter's `constants/upload.dart` already defines `allowedDocumentTypes` that nothing backend-side currently honors, suggesting this was anticipated once and not finished.
*Fix direction:* T28-T34 (backend+web), T37 (Flutter).

**F8 — Emoji picker: fully greenfield on both platforms, nothing to adapt.** Confirmed. Zero emoji-related npm dependency in `next-js-boilerplate/package.json`, zero existing picker component (even unused/demo) in `components/ui/`. The only "emoji" hits anywhere are `ReactionButtons.tsx`'s 4 hardcoded fixed reaction emoji (👍❤️😂😮) — a fixed reaction-selector, structurally unrelated to a free-text insertion picker (no search/categories/unicode dataset). Same on Flutter: no emoji package in `pubspec.yaml`, no picker widget in `lib/components/ui/`. Neither composer (messages or chat-room, either platform) has any emoji affordance today.
*Fix direction:* T35-T36 (web), T38 (Flutter).

### 2.4 Settings/Sessions

**F9 — "No active sessions found" is caused by a blanket error-swallow, not necessarily an empty result.** Confirmed as the proximate cause. `views/settings/sessions/FreePageView.tsx:51-64`'s fetch is wrapped in `try { ... } catch { setSessions([]); }` with **no logging, no telemetry, nothing** — any failure at all (network error, GraphQL error, 401, 500) renders the exact same `EmptySessions.tsx` ("No active sessions found") as a genuine zero-sessions state. The `session`(singular)/`sessions`(plural) split was checked and resolved: `session/` is a dead, unimported NestJS-docs tutorial module; `sessions/` (via `SessionsResolver`) is the real, wired-in feature, and `mySessions` has no duplicate/shadow resolver. Cookie names, header names, and the frontend/backend `SessionInfo` field shapes all match — those plausible theories were checked and ruled out.
*Most likely underlying trigger (Plausible, needs live verification):* `SessionsResolver.mySessions()` (`sessions.resolver.ts:48-76`) makes an **unguarded** `prisma.device.findMany(...)` call inline, with no try/catch, purely to enrich the response with a "Trusted" badge. If that throws for any reason (DB blip, pool exhaustion), it kills the *entire* response — a best-effort enrichment taking down the primary feature. This exact interaction is untested (the resolver's own spec mocks `tokenStore.listSessionsWithKeys` directly and never exercises the Prisma call). Confirming this is actually firing needs live backend log tailing during a repro.
*Fix direction:* T39-T40.

**F10/F11 — Independent, confirmed bugs found in the same area, worth fixing regardless of F9's exact trigger.** Confirmed. (a) `TokenStoreService.extendTTL()` (`token-store.service.ts:148-151`) only slides the main session hash's Redis TTL — the parallel `refresh_sess:<sessionId>` reverse-index key (needed to redeem a refresh token) is set once at login/refresh and never renewed, so a continuously active user's *session* stays alive indefinitely but their ability to *refresh* silently dies after `SESSION_TTL` (default 900s) regardless of activity. This directly undermines Stage A's auth-loop fix if it starts relying on the real refresh mutation — must be fixed together (see F3 in the executive summary). (b) The reverse-index Redis `SET` (`user:<id>:sessions`) is never independently expired — stale members accumulate forever (doesn't cause wrong results, since dead sessions are filtered at read time, but it's unbounded growth). (c) `api/server/sessions/list.ts:4-12` declares its own local `SessionInfo` type with a phantom `expiresAt` field the backend never returns, reconciled only via an `as unknown as` cast that defeats type-checking. (d) The sessions-list BFF route omits `noCache: true` on its `graphqlFetch` call — same cross-cutting caching-default gap called out in the executive summary; `api-keys/route.ts` has the identical gap.
*Fix direction:* T41-T44.

**F12 — Device type is already computed backend-side, just never exposed; IP is already both captured and rendered; "current session" badge is already fully built end-to-end.** Confirmed, and good news on two of three fronts. IP: `main.ts:108` sets `trust proxy: 1`, `session.ip` is already in the GraphQL type and already rendered (`SessionCard.tsx:63-68`) — nothing to do here beyond confirming prod's proxy topology is exactly one hop (can't verify statically). Device type: `device.service.ts:47-65`'s `detectDeviceType()` already computes a clean enum (`WEB`/`MOBILE_IOS`/`MOBILE_ANDROID`/`DESKTOP`/`API`/`CLI`) and stores it on `Device.type` — but `mySessions()`'s Prisma call only selects `{id, trusted}`, and the GraphQL type has no `deviceType` field, so the frontend falls back to crude UA-string-sniffing (`SessionCard.tsx:18-21`) and just truncates the raw UA as a "device name." This is a pure wire-up task, not new data capture. (`Device.name String?` exists in the schema but is never populated anywhere — a dead column if a human-readable "Chrome on MacBook" label is wanted later, out of scope here.) Current-session badge: **already fully implemented** — `auth.resolver.ts:148-166`'s `me` query returns `sessionId`, `FreePageView.tsx:79,111` already computes `isCurrent`, and `SessionCard.tsx:51-98` already renders the badge/border/hidden-revoke-button. This should just start working once F9 is fixed — do not rebuild it.
*Fix direction:* T45 (device type wire-up only); T46 is a verify-only checkbox, no code.

### 2.5 Settings/Billing

**F13 — Critical: paid→paid upgrades never cancel the prior Stripe subscription.** Confirmed. `billing.service.ts:56-169`'s upgrade branch never checks `user.stripeSubscriptionId` and never calls `cancelSubscription()` before creating a new one — that logic exists only in the *downgrade* branch. BASIC→MEDIUM creates a second live Stripe subscription and overwrites the single `stripeSubscriptionId` column (line 108), permanently losing the reference needed to ever cancel the first one in-app. The customer is billed for both, forever. Untested: upgrade specs (`billing.service.spec.ts:106-172`) all start from `FREE`, never from an existing paid tier.

**F14 — Weak idempotency on two independent axes.** Confirmed. `generateIdempotencyKey()` (`billing.service.ts:442-448`) buckets by **60-second** wall-clock windows — a double-click even slightly more than a minute apart produces two different keys, no protection past that window. Within the window it's a classic TOCTOU race: `findFirst` for an existing key, then a full Stripe round-trip, then the write — two concurrent requests can both pass the check before either commits. No Stripe-side `idempotencyKey` request option is passed either, so there's no provider-level backstop.

**F15 — Stripe charge and local state aren't atomic across a crash window, and the webhook doesn't reconcile.** Confirmed (partially good news, partially not). The tier-update and wallet-ledger write *are* correctly wrapped in one `$transaction` together (`billing.service.ts:103-143`) — but that transaction runs *after* the Stripe charge already succeeded. A crash in that gap leaves Stripe billing live with zero local record. The `invoice.paid` webhook (`stripe-webhook.controller.ts:92-169`) fires independently but only updates `subscriptionPeriodEnd`/`stripeSubscriptionId` — **it never touches `subscriptionTier`** — so there's no reconciliation path for this exact gap.

**F16 — Likely duplicate invoice rows per upgrade.** Confirmed as a structural risk. The synchronous path writes a `$0`/`stripePaymentIntentId: null` `WalletTransaction` (`billing.service.ts:113-130`, key scheme `sub:<user>:<tier>:<minuteBucket>`); the async `invoice.paid` webhook writes its *own* `WalletTransaction` for the same event with a completely different key scheme (`stripe_invoice:<invoiceId>`) and no cross-reference to the first. `getBillingHistory()` has no dedup logic, so both rows surface in `myBillingHistory` — a user upgrading likely sees two line items for one purchase, one showing $0.

**F17 — Currency double-division: invoice amounts wrong by ~100x.** Confirmed. `stripe-webhook.controller.ts:97`: `Math.round(invoice['amount_paid'] / 100)` treats Stripe's cents as if converting to whole dollars (also rounding away the cents). `InvoiceTable.tsx:98` then does `` `$${(tx.amount/100).toFixed(2)}` `` — dividing by 100 *again*, assuming the stored value was cents. Given real configured prices (`$9.99`/`$19.99`/`$49.99`, all X.99), a $19.99 charge stores as `20` and renders as **$0.20**. The rest of the codebase's own convention (`lib/currency.ts`'s `formatPrice(cents,...)`, `lib/tier.ts`'s `TIER_PRICES_CENTS`) is "store cents, format at render" — the webhook handler is the one place that deviates from its own codebase's convention. Zero test coverage exists for `stripe-webhook.controller.ts` at all, which is how this went uncaught.

**F18 — "Best-effort" Stripe cancellation can silently diverge from reality.** Confirmed. Both `cancelSubscription()` and the downgrade branch catch-and-log any Stripe cancel failure, then **unconditionally** proceed to write local `cancelAtPeriodEnd: true` regardless. If the Stripe call actually failed, the app tells the user "cancellation scheduled" while Stripe keeps auto-renewing and charging — no retry, no reconciliation, no user-facing indication of the mismatch.

**F19 — Downgrade access-cutoff timing is inverted; paid→paid downgrade never provisions the replacement subscription.** Confirmed. The downgrade branch drops the app-side tier **immediately** while the Stripe subscription (still at the old, higher price) is only flagged `cancel_at_period_end: true` and keeps billing until the period naturally ends — the customer loses access to what they're still paying for, the inverse of the usual "keep access through what's already paid for" pattern. Separately: if the target is still a paid tier (e.g. PREMIUM→BASIC, not →FREE), the code only cancels the old subscription and never creates a correctly-priced replacement — once the old subscription lapses, the account sits on a "paid" app tier with zero active billing, indefinitely.

**Billing restructure (2b/2c), confirmed straightforward:** Invoices currently render as a third full-width block stacked directly beneath the two-column Plan/Payment/Address region (`views/settings/billing/FreePageView.tsx:52-100`) — confirming the "squeezed in" framing exactly. A `Tabs` component already exists and is already used elsewhere with an established convention (`components/ui/Tabs.tsx`, see `views/ui/tabs/SettingsSections.tsx:72-91`) — wrapping the existing two-column region and `InvoiceTable` as two `TabsContent`s is pure composition, no new component needed. `PlanBenefits.tsx:73-95` is currently a plain, always-expanded list with no collapse mechanism; a Radix-backed `Accordion` already exists (`components/ui/accordion/accordion.tsx`) — `type="single" collapsible"` with no `defaultValue` gives collapsed-by-default for free.
*Flutter nuance (Plausible → needs a decision, see §3):* Flutter's actual billing page (`page_view.dart`, routed at `/v1/:lang/settings/billing`) has the same "everything in one long scroll, Invoices last" structure — but it has **no Plan Benefits section of any kind**. That content (`plan_advantages.dart`/`plan_info_card.dart`) lives on a *different* screen, the general Settings hub (`views/settings/page_view.dart`), which isn't competing with Invoices for space. So this isn't a 1:1 port of the same accordion — it's a design decision about whether Flutter's billing page should gain that content first. Both `AccordionWidget` (defaults fully collapsed already) and `TabsWidget` already exist in Flutter's component library regardless, so no new widgets are needed either way.
*Fix direction:* T47-T55 (backend+web), T56-T57 (Flutter, decision-gated).

### 2.6 Settings/General — theme button, info boxes, and the auth loop

**F20 — Theme toggle on settings/general is confirmed-safe to remove; info-box treatment is confirmed inconsistent.** Confirmed. `ThemeToggle` (`views/settings/general/FreePageView.tsx:9,77-79`) is redundant — `V1Header.tsx:6,51` already renders one in the app shell wrapping every `/v1/*` route, settings included, plus it's also present on the `/auth/*` layout and the marketing/forms/UI-gallery layouts. Removing it from settings/general doesn't remove the only way to change theme. The shared "info" affordance across settings pages is `PageInfoButton` (`components/ui/page-info/`), not `Alert` — it opens a `Dialog` (default `sm:max-w-lg`, 512px cap, never overridden) with a "Tips" box (`border-border bg-surface rounded-lg border p-3`) that only renders **if** `content.tips` is non-empty. Of 7 settings pages, only 4 (`api-keys`, `billing`, `sessions`, the settings index) define `tips`; 3 (`account`, `general`, `privacy`) don't; **`security` has no `PageInfoButton` at all** — confirming the "inconsistent, sometimes-rushed" impression precisely: the same dialog renders a materially different shape depending on which page you're on. Separately, no settings-area "box" (this tips box, `PlanInfoCard`, `PlanAdvantages`, billing's hand-rolled cards) actually reuses the app's real `Card` component — each inlines its own `border-border bg-surface rounded-{lg,xl} border p-{3,4,5}`, at three different padding values.
*Fix direction:* T65-T69.

**F21 — THE auth loop, primary mechanism: the automatic-logout path never clears cookies.** Confirmed, traced end-to-end, file:line at every step:
1. Some authenticated request 401s for real (see F23 for concrete triggers) via `SessionAuthGuard`.
2. `apiFetch()` (`lib/api-client.ts:18-29`) sees the 401 and does `window.dispatchEvent(new CustomEvent("auth:logout"))`.
3. The only listener, `onAuthLogout` (`features/auth/hooks/useAuth.tsx:113-130`):
   ```js
   function onAuthLogout() {
     if (logoutEventRef.current) return;
     logoutEventRef.current = true;
     setUser(null); setToken(null);
     const path = window.location.pathname;
     if (path !== "/auth/login" && !path.startsWith("/auth/")) {
       window.location.href = "/auth/login";
     }
   }
   ```
   This clears **only in-memory React state** and hard-navigates. It never calls `logoutServer()`/`/api/auth/logout`, so `access_token`, `rbac_token`, `device_token`, `user_token`, and **`session_user`** all survive completely intact.
4. Fresh full-page load on `/auth/login`. `app/auth/login/page.tsx:15-19`'s server-side check, `getSessionUser()`, finds the still-present `session_user` cookie, **blindly decodes it with no signature/expiry check**, returns a non-null user → immediately `redirect('/v1/en/feed')`.
5. Back on `/v1/en/feed`: `proxy.ts:157` (this app's Next.js middleware — the file is named `proxy.ts`, not `middleware.ts`, per Next 16's renamed convention; there is no `middleware.ts` anywhere in the repo, confirmed via `git log --all`) only checks *presence* of `access_token`, not validity — passes through. `app/v1/[lang]/layout.tsx:16-18` calls the same blind `getSessionUser()` → renders normally, mounting a **brand-new** React tree (full reload resets `logoutEventRef.current` to `false`).
6. The freshly-mounted feed page's queries hit the backend guard again, fail for the *same underlying reason as step 1* (nothing backend-side changed), the whole sequence repeats.
7. The loop only breaks when the shared 15-minute cookie Max-Age finally elapses (counted from original login, unrelated to when the loop started) or the user manually clears cookies.
This is exactly Berkay's own diagnosis — "token/session residuals — it believes session is still active but it is not" — with the specific residual now identified precisely: `session_user` (and its siblings) surviving a logout path that was only ever written to manage client memory, never server-side cookie state. **Mostly deterministic, not a race** — the `logoutEventRef` dedup guard correctly collapses multiple concurrent 401s into one navigation; the actual defect is an incomplete operation (missing cookie-clear step), not two operations racing.

**F22 — The "refresh" mechanism doesn't refresh; the real one is never called.** Confirmed. `useAuth.tsx:86-92` calls `refreshTokenServer()` → `/api/auth/token` (`app/api/auth/token/route.ts`), whose entire body reads the current cookies and echoes them back as JSON — **no backend call of any kind**, cannot mint a new token or detect an expired one. Meanwhile the backend has a fully correct, already-implemented `refresh` GraphQL mutation (`auth.resolver.ts:72-76` → `auth-session.service.ts:52-138`) that correctly revokes the old session and — since the `c0982fa0` fix — correctly carries device identity into a fresh token issuance. Repo-wide grep for a `refresh` GraphQL mutation call from the web frontend: **zero matches.** It's also structurally unreachable even if wired naively: the BFF's login query never requests a `refreshToken` field, and `lib/cookie.ts` has no `REFRESH_TOKEN_COOKIE` concept at all — the value needed to call `refresh` never reaches the browser in the first place. Net effect: **there is no code path that can silently repair a stale web session.** A landmine found in passing: a second, dead-looking file `api/server/auth/refresh-token.ts` exports the *same* function name `refreshTokenServer` with a different signature — `useAuth.tsx` imports from `token.ts`, not this one, but the naming collision is a hazard for future edits.

**F23 — Concrete, code-confirmed triggers for a "valid-looking" cookie to disagree with the backend.** Confirmed (a, b); Plausible-but-real-mechanism (c, needs live check). (a) **Tier change during an open session** — `billing.service.ts:145,234` call `tokenStore.rewriteFieldsForUser(userId, {tier})` on every upgrade/downgrade, which immediately HSETs the new tier onto *every* live Redis session for that user. The guard's step-5 check recomputes `deriveRbacToken(userId, tier)` fresh from that Redis value — the very next authenticated request after a plan change has a guaranteed mismatch against the `rbac_token` cookie (still derived from the old tier at login), 401ing on the spot, cookies otherwise untouched and still within their 15-minute Max-Age. `useAuth.tsx:200-213`'s `onTierChanged` WS handler exists specifically to keep the UI in sync post-tier-change but only refetches the *display* user object — never touches the `rbac_token` cookie, so it doesn't prevent this. Directly relevant: the billing page under investigation in Stage F is exactly the trigger for this. (b) **UTC-midnight derivation cutover** — `TokenDerivationService.dateOnly()` truncates to the UTC calendar date for both `user_token` and `rbac_token`; any session opened in roughly the last 15 minutes before UTC midnight has cookies still present past midnight but derived for "yesterday," rejected as "Daily token expired." (c) Explicit session revocation while local cookies persist — a real mechanism (`revoke`/`revokeAllForUser`), not confirmed to currently misfire; would need a live check.

**F24 — The explicit "Sign out" button has a milder, related bug.** Confirmed. `logout()` (`useAuth.tsx:185-189`) correctly `await`s `logoutServer()`, which properly clears all 5 cookies server-side regardless of whether the backend revoke succeeds. But **nothing downstream navigates anywhere** — confirmed at every call site (`ProfileDropdown.tsx:49-53`, `ProfileSection.tsx:60-64`, both fire-and-forget, no `router.push`/`redirect` in the chain). Clicking "Sign out" correctly clears everything server-side but leaves the user staring at the same URL until some component's own inline `if (!user)` check happens to kick in. Not a loop, but the same family of bug: an auth transition that's incomplete about the "then what" step.

**F25 — Two recent commits already attempted a narrower, related fix; never verified.** `3aff834d`/`c0982fa0` (2026-07-28, both **after** convert-frontend-9's doc closed clean, so outside any verification round in that doc) fixed a session-key-mismatch specific to the MFA-verify path: `verifyLoginMfa` previously called `issueTokens(user, ctx)` **without resolving device context**, unlike every other login path — meaning a successful MFA login could mint a session keyed on an empty device-token slot the next real request could never match. The fix (peek-not-consume the MFA challenge + `this.devices.resolveForLogin(...)` before `issueTokens`) is now consistent with all 4 other `issueTokens` call sites (checked directly: `login()`, `loginWithOAuth()` ×2, `refresh()` — all correctly resolve/carry device context today). Ran `npx jest src/auth/auth.service.spec.ts src/auth/auth-session.service.spec.ts` live this session: **20/20 pass.** This is a real, distinct fix from F21/F22 — it addresses one specific call site's device-context bug, not the general cookie-cleanup gap. Both should be swept into the same verification pass since they're both in the auth trust chain, and this one was never independently confirmed live (only via unit tests, which mock heavily).

**Flutter comparison — structurally not affected by F21/F22.** Confirmed. `AuthNotifier` (`hooks/use_auth.dart`) is a single `StateNotifier`, watched identically by the router's redirect guard, the Dio interceptor, and every widget — one source of truth, can't disagree with itself the way web's proxy/layout/React-context triad can. `AuthInterceptor.onError` (`lib/lib/api_client.dart:88-130`) already catches 401s and calls a real `refreshAccessToken()` that hits the backend's actual refresh endpoint and persists rotated tokens — a real, working refresh flow. **This directly contradicts the `flutter-conversion` skill doc's current claim ("There is no token-refresh flow yet")** — that's stale, worth correcting (T70). Two narrower, not-currently-symptomatic risks worth carrying forward: `_isRefreshing` is a plain boolean, not a queue, so concurrent 401s beyond the first fail without retrying; and Flutter's refresh chain shares F11's non-sliding `refresh_sess` TTL backend-side, so it must complete at least once per 900s window under active use (should self-sustain in practice, not independently verified live).
*Fix direction:* T1-T8.

### 2.7 Feed comments

**F26 — Replies are structurally excluded from the feed's post query — permanent, not timing-dependent.** Confirmed. `PostService.findOne()` (`post.service.ts:185-243`) queries `comments: {where: {deletedAt:null, parentId:null}, ...}` — an unconditional filter that can never return a reply, at any TTL, after any number of reloads — and doesn't even `select: {parentId:true}`, so the field wouldn't come through even if the filter were relaxed naively. The Prisma schema and GraphQL model both correctly support replies (`Comment.parent`/`replies`, `@@index([parentId])`); this one query just doesn't use them. **The backend already has a correct query**: `CommentService.findByPost()` (exposed as `postComments(postId)`) properly nests replies — but grep confirms the web app **never calls it**, only the broken `post(id){comments{...}}` path. Client-side symptom: an optimistic reply renders immediately, then is removed the instant the mutation resolves and never replaced, since the follow-up refetch can structurally never contain it — a user watches their own reply vanish. (A second reply to the same parent then 409s as a duplicate, since the first one *did* persist — a likely source of real user confusion worth noting.)

**F27 — Three-layer, unbusted cache stack sits in front of the same read path, and the feed page specifically has no working invalidation at all.** Confirmed. Layer 1: the BFF GET route sets `Cache-Control: public, max-age=60, stale-while-revalidate=120` (`app/api/posts/[id]/route.ts:38-45`) — a real browser cache hit is likely given the natural "expand comments → post → recheck" flow. Layer 2: that route's own outbound `graphqlFetch` call omits `noCache`, taking the `next:{revalidate:60}` branch — this is server-side and **invisible to a browser reload**, directly explaining "not even after a full reload." Layer 3: the backend's Redis cache-aside (`PostService.findOne`) *is* correctly invalidated by both `create()`/`update()` (`comment.service.ts:70-71,107-108`) — not itself broken, just dominated by layers 1-2 which are never invalidated on any timescale shorter than their TTL. Compounding, feed-page-specific problem: `PostCard.tsx`'s `postData` is plain `useState`, not react-query-observed — so neither the manual invalidation (`usePostActions().invalidate()`, only touches `["feed"]`/`["posts"]` prefix keys) nor the realtime-driven one (`{renew:'Feed',type:'Post',id}` → `qc.invalidateQueries({queryKey:["posts",id]})`) ever reaches it, since nothing on the feed page observes that query key at all — `refetchType:"active"` is a no-op against zero active observers. Both invalidation paths *do* correctly exist and *do* fire — they're just aimed at a query nothing on this specific page mounts. (The standalone `/posts/[uuid]` detail page is wired correctly against the matching key, but ultimately calls the same poisoned two-layer read path, so likely shows the same staleness for a shorter effective window.)

**F28 — Edit doesn't refresh because the mutation's own response is discarded, then the same poisoned refetch runs.** Confirmed. The backend's `update()` (`comment.service.ts:85-120`) is real — ownership-checked, calls Prisma's `.update()`, returns exactly the shape the frontend's mutation requests, and is covered by a passing unit test. The frontend's `updateCommentServer` (`api/server/posts/comments.ts:22-35`) never reads or returns the response body at all (`Promise<void>`) — there's no cache reconciliation of any kind for this app's data layer (react-query, but this state isn't under it per F27). The only path back to fresh data is the same `onCommentAdded()` → `refreshPost()` refetch F26/F27 already poison. Second-order effect worth noting for the exact reported symptom shape: `CommentSection.tsx`'s optimistic `localEdits` overlay shows the new text immediately, the mutation succeeds, the overlay clears, then the un-awaited stale refetch resolves and overwrites parent state with the *old* text — a user perceives "my edit didn't save" when it actually did; the read path lied about it.

**F29 — Apollo Federation ruled out.** Confirmed, a dead end. `app.module.ts` wires a single plain-`ApolloDriver` `GraphQLModule` (not `ApolloFederationDriver`); `federation/posts-subgraph/` is a self-contained, unrelated NestJS-docs example backed by a hardcoded in-memory array of 3 fake posts, with its own separate `GraphQLModule.forRoot`, never imported into the real `AppModule` bootstrap. No subgraph boundary exists in the live system — not a contributing factor.

**F30/F31 — Flutter shares the symptom shape, not the mechanism.** Confirmed. Flutter talks directly to the backend over Dio with no BFF layer, so F27's caching stack doesn't exist on mobile — but `postComments`'s Dart-side GraphQL selection (`api/server/posts/comments.dart:10-21`) only asks for `{id,body,authorId,postId,createdAt,updatedAt}` — no `parentId`, no nested replies — so replies are invisible on mobile too, for a client-query-gap reason rather than a backend-filter reason (the Dart model already has a `parentId` field, just unpopulated — an easier fix than web's, since the backend query Flutter already calls is the *correct* one). Separately, none of the three comment-mutation call sites (`add`/`update`/`delete` in `api/client/posts/actions.dart:51-67`) ever call `ref.invalidate(postCommentsProvider(...))` — the **only** thing that can refresh that provider is a realtime WS push (`realtime_provider.dart:60-92`, correctly wired when it fires). This makes Flutter's comment refresh realtime-or-nothing: correct and immediate when the socket is live, silently stale if the socket is down/reconnecting at the moment of the mutation — a plausible explanation for an intermittent-on-mobile-specifically version of this bug, flagged as needing a live WS-traffic check to confirm reliability.
*Fix direction:* T58-T62 (backend+web), T63-T64 (Flutter).

---

## 3. Open decisions needing Berkay's sign-off before implementing

1. **Auth refresh strategy (F22, T3).** Two directions, different sizes: (a) minimal — wire the frontend to actually call the backend's existing `refresh` mutation on 401, mirroring Flutter's `AuthInterceptor` pattern; (b) larger — reconsider the 4-piece compound session key (`access+rbac+device+user` tokens, independently derivable/expirable) in favor of a single opaque session identifier the backend resolves server-side, removing the entire class of "one piece drifted out of sync" bugs this key design has produced at least twice before (this doc's F25, and `refresh()`'s device-identity drop fixed in convert-frontend-7). Berkay's "mature change... in auth mechanism" phrasing suggests (b) might be the intent — worth confirming before scoping Stage A's implementation size.
2. **Chat nickname scope (F4, T16).** Should the nickname replace the real name in DMs too, or only in chat-room presence lists? Determines whether `displayName()` gets a parallel "chat display name" helper or becomes tier-aware in place.
3. **`MINIO_PUBLIC_URL` value (F5, T23).** The plausible value already sitting in three unused env locations (`https://app.eys.gen.tr/api`) needs confirmation that the external openresty proxy actually forwards `/api/uploads/*` to MinIO before committing to it — an infra check outside this repo, not resolvable statically.
4. **Billing fix prioritization (F13-F19).** These carry real financial exposure if this environment ever processes live (non-test-mode) Stripe charges — worth confirming priority/sequencing with Berkay explicitly rather than treating all 7 as equally urgent; F13 (orphaned duplicate subscriptions) and F17 (100x currency error) are the two with direct, ongoing dollar impact.
5. **Flutter billing parity (F19-sidebar, T56).** Does Berkay want Flutter's billing page restructured to match web's layout (adding a Plan Benefits section there for the first time), or leave Flutter's existing hub-page-based split as-is since it isn't fighting Invoices for space there?
6. **Screenshots.** [Image #1]/[Image #2] never reached this investigation — §2.6/Stage H's UI recommendations are code-derived, not screenshot-derived. Sanity-check before implementing.
7. **Cross-cutting BFF caching default** (executive summary). Patch each affected call site individually (T44, T59, T60 as scoped below) vs. flip `graphqlFetch`'s shared default to opt-in caching instead of opt-out — the latter is more work now but forecloses this exact bug class recurring at the next per-user mutable endpoint someone adds.

---

## 4. Proposed fix & build plan

All task boxes T1–T73 are checked. Stages A–H (T1–T69) are complete and unit-verified; Stage I's T70/T71/T73 were done via earlier stages and T72 (MockPaymentProvider) was deleted per Berkay 2026-07-31. Live-only verification items remain deferred to the §5 live gate: Stage A's T6 (real login+MFA round-trip), Stage C's live/infra checks, Stage D's live upload/emoji round-trips, and Stages F/G's live Stripe/comment round-trips — each §5 box carries a per-box note with its partial-completion status and the specific blocker (no docker stack / VAULT_TOKEN / Stripe test keys on this server). Ordered by stage priority; within a stage, roughly by dependency order.

### 4.1 Stage A — Critical: structural auth session/redirect-loop fix

- [x] **T1** — Make `onAuthLogout` (`useAuth.tsx:113-130`) perform the same complete, `await`ed cleanup the manual `logout()` path already does (clear all BFF cookies via `/api/auth/logout` or equivalent) *before* navigating, not after or never. (F21) — implemented 2026-07-31: `onAuthLogout` now `await`s `logoutServer()` before `redirectToLoginIfNeeded()`; cookies cleared before the full page load hits the login page's server-side check.
- [x] **T2** — Give the manual `logout()` path (`useAuth.tsx:185-189`) a navigation step — it currently clears everything server-side correctly but never redirects anywhere. (F24) — implemented: `logout()` calls the shared `redirectToLoginIfNeeded()` after clearing state; both call sites are fire-and-forget, verified safe.
- [x] **T3** — Decide and implement a real refresh flow per §3.1: either wire the frontend to call the backend's existing `refresh` mutation on 401 (mirroring Flutter's interceptor), or explicitly redesign the session model. Needs Berkay's direction before sizing. (F22) — **minimal path per Berkay**: `refresh_token` cookie set on login/mfa/register (30-day, mirrors backend), new BFF route `/api/auth/refresh` (presents cookie via `x-refresh-token` + CSRF echo + old access token as Bearer for compound-key revocation, rotates all 4 session cookies + refresh token, `noCache: true`), and `apiFetch` now does one single-flight refresh-and-retry on 401 before dispatching `auth:logout`. `session_user` deliberately untouched (identity unchanged by rotation).
- [x] **T4** — Fix `TokenStoreService.extendTTL()` to also slide `refresh_sess:<sessionId>`'s TTL, not just the main session hash — otherwise T3's refresh flow silently stops working 15 minutes after login regardless of activity. Same underlying fix as T41 (Stage E) — implement once. (F11) — implemented + 2 new spec tests. Also repaired the pre-existing broken spec mock (missing `multi().set`, `hget`, `get` — 6 of its tests were failing on clean main).
- [x] **T5** — Add reconciliation for the tier-change trigger (F23a): push a fresh `rbac_token` to the client on tier change (extend the existing `onTierChanged` WS handler, which today only refreshes the display user) rather than leaving every tier change as a guaranteed next-request 401. — implemented: `onTierChanged` now fires `refreshSession()` (reuses T3's machinery; the backend `refresh` mints rbac from the current DB tier) then refetches the display user. The refresh-on-401 path remains the safety net when the WS frame is missed.
- [ ] **T6** — Live-verify (not just unit-test-trust) commits `3aff834d`/`c0982fa0`'s MFA-specific device-context fix with a real login+MFA round-trip — it shipped after convert-frontend-9 closed and was never independently checked. (F25) — **unit-verified 2026-07-31 (20/20 auth specs pass, confirmed live); the real login+MFA round-trip is blocked — no docker stack running on this server — deferred to the §5 live round-trip gate.**
- [x] **T7** — Correct the `flutter-conversion` skill doc's stale "no token-refresh flow yet" claim; Flutter's `AuthInterceptor` already implements one correctly. (Flutter comparison, F21-F25 section)
- [x] **T8** — Add a regression test exercising "expired/invalid session → clean recovery or redirect, no loop" end-to-end on web — none exists today. — added `src/lib/__tests__/api-client.test.ts` (6 tests): refresh-then-retry, refresh-fails→single `auth:logout` (no storm), retry-still-401→logout, single-flight concurrency, unchanged POST bodies, pass-through of non-401.

**Definition of done:** a session made stale by any of F23's triggers redirects to login exactly once and cleanly re-authenticates on the next real login, with no bounce back to feed; `refresh_sess` keys observably slide under active use; T6's live MFA round-trip passes; a new test (T8) locks in the no-loop behavior.

### 4.2 Stage B — Messaging & chat-room bugs

- [x] **T9** — Change the 3 friend-request handlers in `messaging.controller.ts` (send/accept/decline) from `emitToPage(..., 'friend-request', ...)` to the same non-page-scoped `emitToService(userId, 'MESSAGE'|'NOTIFICATION', {renew:'Friends', type:'PendingList'})` pattern already used for notifications/new-messages. (F1) — verified: all 3 handlers (`messaging.controller.ts:91-136`) now `emitToService(userId, 'MESSAGE', {renew:'Friends', type:'PendingList'})` for both parties, with a comment documenting the page-scoping bug this replaced.
- [x] **T10** — Confirm no other consumer depends on the current page-scoped Friends semantics before removing it. — verified: `route-mapping.ts:16` still claims the `"friend-request"` page for `/v1/en/find-friends` (harmless page claim); no consumer anywhere reads a page-scoped `friend-request` frame — the web side consumes service-scoped `{renew:'Friends'}` frames via `renew-dispatch.ts`, which both web and Flutter handle regardless of page. Safe to mark complete.
- [x] **T11** — Guardrail while implementing T9: keep `selectedUser` exactly as today's isolated local state; never derive it from the newly-invalidated `["friends","list"]`/`["conversations"]` data. (F2) — verified, no code needed: `selectedUser`/`selectedConversationUserId` is isolated local state everywhere (web `useMessagesPage`/`useChatRoomRealtime` selection, Flutter `selectedConversationUserIdProvider`); the T20 invalidation refetches the list, never the selection.
- [x] **T12** — Move chat-room's scroll-to-bottom sentinel div from `ChatRoomMainContent.tsx` (currently a sibling outside the scrollable region) into `ChatRoomMessageList.tsx` as the scrollable div's last child, mirroring `ChatMessageList.tsx`'s already-correct structure. (F3) — verified: sentinel `<div ref={bottomRef} className="h-px" />` is now the last child of the scrollable list inside `ChatRoomMessageList.tsx:85`; `ChatRoomMainContent.tsx:97,102` passes `bottomRef` down and keeps only the scroll-to-bottom button.
- [x] **T13** — Add a chat-nickname field to the `User` Prisma model + migration. (F4) — implemented: `prisma/migrations/20260731000000_add_chat_nickname/migration.sql` (`ALTER TABLE "User" ADD COLUMN "chatNickname" VARCHAR(30);`); `prisma validate` + `prisma generate` green.
- [x] **T14** — Add it to `UpdateProfileInput`/`ProfileService.updateProfile`. — implemented: `chatNickname?` with `@MinLength(1)`/`@MaxLength(30)` (empty string = not set); `updateProfile` persists to DB and mirrors the value into the Redis session hash (`redisFields.chatNickname`) so the next socket auth picks it up.
- [x] **T15** — Wire `settings/privacy/FreePageView.tsx`'s `handleSave` to an actual API call (currently discards its payload entirely) plus add the missing initial-value fetch. — implemented: `handleSave` calls `updateProfile` with the nickname (empty clears it), toasts `saveSuccess`/`saveFailed`; toggle + field seeded from `user.chatNickname`. `hideProfilePicture` remains a local-only toggle (no backend field exists).
- [x] **T16** — Thread the nickname into the session hash so the realtime gateway can resolve a chat display name — likely a small parallel helper alongside `displayName()`, scope per §3.2's open decision. — implemented (chat-room-only scope per §3.2): `SessionUser`/`SessionUserPayload`/`JwtUser` types + `token-store` HSET write (`chatNickname: data.chatNickname ?? ''`), v1-backward-compat read returns `chatNickname: ''`, `session-hydration.hydrate` snapshot, `auth.resolver.me` returns it.
- [x] **T17** — Add the field to the `RoomMember` shape and both `user-joined`/`user-left` construction sites in `messaging-ws.gateway.ts`. — implemented: `RoomMember.chatNickname?`; both join sites pass `chatNickname: ws.chatNickname || undefined` (set at socket auth in `realtime.gateway.ts`); `room-message` payload `senderName`/`avatar` prefer `chatNickname`.
- [x] **T18** — Have `ChatRoomSidebar.tsx` prefer the nickname if present. — implemented: sidebar renders `chatNickname || name` for avatar + label; also fixed a pre-existing latent bug in `useChatRoomRealtime.ts` — members were cast as `{id,...}` but the backend sends `userId`, so presence crowns/online dots never matched (mapping now handles both).
- [x] **T19** — (Flutter) Build the missing accepted-friends list UI — `messages_sidebar_friends.dart` is currently a hardcoded empty stub, no `friendsProvider` exists yet; T9's fix is only meaningful on mobile once this exists. — implemented: rebuilt on the existing `friendsListProvider` with search filter, online/offline indicators, conversation selection; shared `OnlineAvatar` extracted from the conversations sidebar (was a private duplicate).
- [x] **T20** — (Flutter) Add a `case 'Friends'` invalidation target for the new accepted-friends list once T19 exists (today it only invalidates `friendRequestsProvider`). — implemented: `handleRenewFrame` Friends case now also `ref.invalidate(friendsListProvider)`.
- [x] **T21** — (Flutter) Wire `privacy/page_view.dart`'s `_save()` the same way as T15 — identical discard-the-payload bug. — implemented: `_PrivacySettings` → `ConsumerStatefulWidget`; `_save()` calls `profileActionsProvider.update(chatNickname:)` (empty clears), toasts `settingsSaveSuccess`/`settingsSaveFailed`; toggle + field seeded from `currentUserProvider`; server layer maps empty→null.
- [x] **T22** — (Flutter) Apply T16-T18's nickname-in-presence change to `chat_room_sidebar.dart` too. — implemented: member name = `chatNickname ?? name`, crown match via `userId ?? id` (backend sends `userId`).

**Stage B verification (2026-07-31):** backend `nest build` green (incl. fallow gate — pre-existing complexity hotspots re-surfaced by line shifts; addressed via `nest-js-boilerplate/.fallowrc.json` `health.thresholdOverrides`, user-approved, no `// fallow-ignore` comments) + 27 targeted spec tests green (profile.service, token-store, session-hydration; `messaging-ws.gateway.spec.ts` doesn't exist). Web `typecheck` + eslint green; 3 security `PageContent.test.tsx` failures verified pre-existing on clean worktree. Flutter: `flutter analyze` clean, 9 new tests green (privacy save/seed/clear, chat-room sidebar nickname + crown, friends sidebar list/filter/empty, realtime Friends invalidation); `flutter test` full suite green except `card_test.dart` `supports onTap` — verified pre-existing on clean worktree at `f6aeb1ca`.

**Definition of done:** accepting a friend request while on `/v1/en/messages` shows the new friend in the sidebar without a reload, without changing the currently-open conversation; sending a message in chat-room scrolls the view down; saving a chat nickname in Privacy actually persists and is reflected in chat-room's Online list on next connect, on both platforms.

### 4.3 Stage C — Avatar/S3 upload fix

- [x] **T23** — Add `MINIO_PUBLIC_URL` to the backend's Vault secret (`docker/vault-init/entrypoint.sh`'s `backend` section) with a real public FQDN — confirm the reverse proxy actually routes `/api/uploads/*` to MinIO first (§3.3, live/infra check). (F5) — repo-side done: `MINIO_PUBLIC_URL=https://app.eys.gen.tr/api` added to `.vault-envs/backend.env` (the compose/Vault mirror; gitignored). The live reverse-proxy check (`app.eys.gen.tr/api/uploads/*` → MinIO) remains deferred to the §5 live gate — if the proxy doesn't route that path, the public URL must be swapped to the real uploads FQDN in the Vault secret.
- [x] **T24** — Relax `UpdateProfileInput.avatarUrl`'s `@IsUrl()` validation (`require_tld: false` or a custom validator) so T23's fix isn't itself rejected; confirm this doesn't weaken validation elsewhere if the DTO/decorator is shared. (F6) — done: `@IsUrl({ require_tld: false })` (`update-profile.input.ts:43`). Grep confirms `@IsUrl` appears in exactly one file in `src/` — no shared DTO/decorator impact.
- [x] **T25** — Surface the specific validation failure message instead of the generic "Validation failed" toast (`graphqlErrorBody()` currently drops the field-specific `fields` array). — done: `GraphQlError.extensions` widened to carry `exc`/`msg`/`key`/`fields`; `graphqlErrorBody()` (`lib/backend.ts:220-248`) now reads `extensions.fields` (the backend's `validationExceptionFactory` emits one entry per constraint) and returns `msg` = the field detail ("Validation failed" → `avatarUrl must be a URL`), plus the raw `fields` array on the response body. All 40+ BFF call sites share this — one change fixes every route.
- [x] **T26** — Resolve the dead `invalidateQueries(["profile"])` no-op — either wire a real consumer or remove the call. — done: removed. Grep confirmed `queryKey: ["profile"]` had zero subscribers anywhere (views fetch via raw `useEffect`); `useProfileActions` no longer carries the unused `useQueryClient` dependency.
- [x] **T27** — (Flutter) Fix `_profileProvider` never being invalidated after a successful avatar save (`page_view.dart`) — add `ref.invalidate(_profileProvider)` (and ideally refresh `currentUserProvider`), mirroring what `refreshUser()` already does on web. — done: `_pickAvatar` now `ref.invalidate(_profileProvider)` after the update, then fetches `me` via the existing `MeServer` and pushes it through `AuthNotifier.updateUser()` so the navbar/header avatar refreshes too (non-fatal if `me` fails — the page refetch still reflects the new avatar).

**Stage C verification (2026-07-31):** `nest build` green (incl. fallow gate); profile specs 9/9; web `typecheck` + eslint on touched files clean, api-client tests 6/6; Flutter `analyze` clean + 65 tests green across `test/views/settings` and `test/lib`. Full backend suite: 379 pass / 8 fail — the 8 are pre-existing stale-spec-mocks on clean main, reproduced identically with the Stage C change stashed (billing `subscribeToPlan` ×4, device `resolveForLogin` ×3 — `prisma.device.count is not a function`, messaging-dm `sendAndDeliverMessage` ×1); source files in all three areas are byte-identical to HEAD, so the failures cannot originate from this work. Live confirmation still owed: reverse-proxy `/api/uploads/*` routing (T23) and a real avatar-upload round-trip (§5 gate).

**Definition of done:** uploading a new avatar shows it immediately on the settings page, navbar, and profile dropdown without a manual reload, on both platforms; the returned URL resolves in a real browser.

### 4.4 Stage D — New features: message file attachments + emoji picker

- [x] **T28** — Add `attachmentUrl`/`attachmentType`/`attachmentName` (nullable) to `Message` and `RoomMessage` + migration. — done: `prisma/schema.prisma` both models; migration `20260731000001_add_message_attachments`; `prisma validate`/`generate` OK.
- [x] **T29** — Parameterize `upload.controller.ts` with a `purpose` (avatar vs. message-attachment) and a document-content-type branch that skips forced image resizing. — done via a separate `POST /upload/attachment` endpoint instead of a `purpose` param: `ALLOWED_ATTACHMENT_TYPES` (images + pdf/doc/docx/txt), `ATTACHMENT_EXTENSIONS`, `FileTypeValidator` with `skipMagicNumbersValidation: true` (legacy formats like docx/txt have no reliable magic bytes), raw MinIO put (no `ImageService.resize()`); returns `{url, originalname, mimetype, size}`. Web BFF `src/app/api/upload/attachment/route.ts` (10MB cap, allowed-types list) + `UPLOAD_ATTACHMENT_URL` const + `MAX_ATTACHMENT_SIZE`/`ATTACHMENT_ACCEPT` in `src/constants/upload.ts`; client server-layer `src/api/server/messages/upload-attachment.ts` (`uploadAttachmentServer` + `toMessageAttachment`).
- [x] **T30** — Add `attachmentUrl` to `SendMessageInput`/`SendMessageRestDto` and to the WS payload types + broadcast construction in `messaging-ws.gateway.ts` (both direct-message and room-message handlers). — done: DTOs gained `attachmentUrl` (`@IsUrl({ require_tld: false })`)/`attachmentType` (max 50)/`attachmentName` (max 255), all optional; `text` also made optional (attachment-only messages allowed, `recipientId` stays required). WS gateway: `IncomingMessagePayload` interface + module-level `toAttachment()` helper; both `direct-message` and `room-message` handlers accept and re-emit the attachment fields; `getRoomMessages` mapper includes them.
- [x] **T31** — Thread the new field through `MessagingDmService`/`MessagingRoomService`'s create calls. — done: `MessageAttachment` interface in `messaging.types.ts`; `MessagingDmService.sendMessage`/`sendAndDeliverMessage`, `MessagingRoomService.saveRoomMessage`, facade, resolver (attachment object), REST controller; `text = ''`/`body = ''` defaults so attachment-only messages persist.
- [x] **T32** — Wire the existing disabled paperclip button in `ChatInputBar.tsx` (`/v1/en/messages`) to an upload-then-attach flow. — done: hidden file input (accept `ATTACHMENT_ACCEPT`), `useMessageUpload()` hook (`src/api/client/messages/actions.ts`), `pendingAttachment`/`attaching` state, upload-progress "Uploading…", pending-attachment chip with remove, send enabled on attachment-only; `chatViewHandleSend` gained `setPendingAttachment`/`attachment` params and clears after send; `validators/messages/schema.ts` text now optional (max 5000, trim).
- [x] **T33** — Add a new attach-file button from scratch to `ChatRoomSubComponents.tsx`/`ChatRoomMainContent.tsx` (`/v1/en/chat-room`) — separate transport (WS) from T32, not a copy-paste. — done: `AttachButton` (hidden-input + label, `useId`)/`EmojiButton`/`PendingAttachmentChip` in `ChatRoomSubComponents.tsx`, ref-forwarding `MessageInput`; composer row in `ChatRoomMainContent.tsx`; `ChatRoomBaseView.tsx` state/wiring; `ChatRoomHandlers.chatRoomHandleSend` sends attachment fields in the WS frame + optimistic message.
- [x] **T34** — Add attachment rendering to `ChatMessageBubble.tsx` and `ChatRoomMessageList.tsx`. — done via shared `src/components/AttachmentPreview.tsx` (image thumbnail vs. document chip, both open in a new tab); body bubble hidden when empty; `ChatRoomMessage` shared type with attachment fields.
- [x] **T35** — Select and install an emoji library for web (e.g. `emoji-mart`/`emoji-picker-react`) and build a new `components/ui` picker component. — done: `emoji-mart@5.6.0` + `@emoji-mart/data`; `src/components/ui/emoji-picker/emoji-picker-button.tsx` (`new Picker({...})` custom element mounted inside the hand-rolled Popover; `theme` via `useTheme()`; `onEmojiSelect` closes via `usePopover`), index + PascalCase shim + `src/types/ui/EmojiPickerButton-types.ts`; demo page `/v1/:lang/ui/emoji-picker` + i18n keys (en/tr).
- [x] **T36** — Wire an insert-at-cursor emoji button into both `ChatInputBar.tsx` and `ChatRoomSubComponents.tsx`. — done: emoji button + insert-at-cursor in both composers; `chatRoomHandleSend` unaffected (emoji is just text).
- [x] **T37** — (Flutter) Backend work (T28-T31) is shared; add Flutter's own composer attach buttons (no placeholder exists on either screen) and attachment rendering. — done: `MessageAttachment` type (`lib/types/messages/message_attachment.dart`); attachment fields on `ChatMessage`/`ChatRoomMessage`/`RoomMessage` (+ `fromJson`); `UploadAttachmentServer` (Dio FormData → `/upload/attachment`, mirroring `upload_avatar.dart`) + `MessageActions.uploadAttachment`/`sendMessage(..., {attachment})`; `SendMessage` GraphQL mutation selects + sends the attachment fields; DM `ChatInputBar` paperclip/emoji buttons + pending chip (size-capped at `ChatConstants.maxAttachmentSize`, toast errors); room composer (`ComposerIconButton` in `chat_room_sub_components.dart`, pending chip + attach/emoji in `ChatRoomMainContent`); `ChatRoomBaseView._pickAttachment` + `_handleSend` threads attachment fields into both `room-message`/`direct-message` WS frames and clears after send; shared `AttachmentPreview` widget (`lib/components/ui/attachment_preview/`) renders in `ChatMessageBubble` and `ChatRoomMessageList` (attachment-only messages hide the body pill).
- [x] **T38** — (Flutter) Select an emoji package (e.g. `emoji_picker_flutter`) and wire it into both composers. — done: `emoji_picker_flutter ^4.5.3`; `EmojiPicker` widget in `ChatInputBar` and `ChatRoomMainContent` (260px popover, insert-at-cursor via controller selection, closes on select).

**Stage D verification (2026-07-31):** backend `npm run build` green (fallow default gate clean; single above-threshold finding is the pre-existing realtime gateway complexity); messaging suites 12/12 (`messaging-dm.service.spec.ts` stale mock also fixed — its create data now expects the real sender+recipient include shape). Web: `pnpm typecheck` clean; `pnpm build` green (with `.vault-envs/frontend.env` sourced); `pnpm test src/api src/lib/realtime` 53/53; pre-existing failures reproduced on clean worktrees: eslint 2 errors (`useMessagesPage.ts:87` set-state-in-effect, `ImagePreviewSection.tsx:24` react-compiler mutation) and the full-test "Invalid hook call" environment failure (12 suites/60 tests). Flutter: `flutter analyze` clean, `flutter test` 437 pass / 1 fail (`card_test.dart` onTap — pre-existing, reproduced on clean main). Live verification still owed (§5 gate): real attachment upload round-trip on both platforms and the emoji picker on a device/browser.

**Definition of done:** a user can attach a file and send it from both `/v1/en/messages` and `/v1/en/chat-room`, on both platforms, and recipients see it rendered; an emoji picker is reachable from both composers on both platforms and inserts at the cursor.

### 4.5 Stage E — Settings/Sessions fix + enrichment

- [x] **T39** — Fix the blanket `catch { setSessions([]); }` in `views/settings/sessions/FreePageView.tsx` to distinguish a real error from a genuine empty list — at minimum log the error. (F9) — done: fetch extracted to a module-level `loadSessions()` (logs via `console.error`); new `sessionsError` state renders a "Failed to load sessions." + "Try again" (retry re-runs `loadSessions`) instead of silently showing `EmptySessions`; i18n keys `sessionsLoadFailed`/`sessionsRetry` added (en/tr). The `as unknown as SessionInfo` cast is gone — the server layer now imports the shared `@/types/settings/SessionInfo-types` type.
- [x] **T40** — Wrap `SessionsResolver.mySessions()`'s inline `prisma.device.findMany()` in its own try/catch so device enrichment can't take down the whole list. — done: best-effort try/catch with `Logger.warn` (includes userId + message); new spec test asserts sessions still return when the device query rejects.
- [x] **T41** — Fix `extendTTL()` to slide `refresh_sess:<sessionId>` too — same fix as Stage A's T4, implement once. (F11) — done via T4: `token-store.service.ts:160` now `pipe.expire(REFRESH_INDEX_PREFIX + sessionId, ttl)` in the same multi as the main hash slide; covered by 2 new spec tests.
- [x] **T42** — Pair the reverse-index `SADD` with an `EXPIRE` on `user:<id>:sessions` (memory hygiene, low priority). — done: `store()` now `pipe.expire(reverseIndexKey(userId), ttl)` right after the `SADD` (dead members are filtered at read time, but the set now shrinks); new spec test asserts the session key + reverse-index expiry in one multi.
- [x] **T43** — Reconcile the divergent `SessionInfo` type definitions (drop the phantom `expiresAt`, remove the `as unknown as` cast). — done: `api/server/sessions/list.ts` no longer declares its own interface (with phantom `expiresAt`) — it imports the shared `@/types/settings/SessionInfo-types`; `src/api/index.ts` re-exports from the shared types path; `FreePageView` dropped its cast.
- [x] **T44** — Add `noCache: true` to the sessions-list BFF route's `graphqlFetch` call; also check/fix `api-keys/route.ts`'s identical gap (cross-cutting pattern, §3.7). — done: both `app/api/sessions/list/route.ts` GET and `app/api/api-keys/route.ts` GET now pass `noCache` (5th positional arg).
- [x] **T45** — Select `type` in `mySessions()`'s device query, add `deviceType` to the `SessionInfo` GraphQL type, have `SessionCard.tsx` prefer it over UA-string-sniffing. (F12) — done: resolver selects `{id, trusted, type}` and maps `deviceType` (module-level `mapSessionInfo` helper keeps the complexity gate clean); GraphQL `SessionInfo` gained the nullable `deviceType` field; BFF query + shared type + barrel updated; `SessionCard` uses `deviceType` (`MOBILE_IOS`/`MOBILE_ANDROID` → mobile icon) with UA sniffing only as a fallback when the field is absent.
- [x] **T46** — Verify-only: confirm the "current session" badge (already fully implemented, F12) starts working once T39/T40 land — no new code expected. — verified by code walk: `me.sessionId` → `isCurrent` → badge/revoke-hide wiring was already complete; with T39 the list no longer gets swallowed to `[]` on any fetch/enrichment failure, so the current session (always present in `mySessions`) renders with its badge. Live confirmation deferred to §5.

**Stage E verification (2026-07-31):** backend `npm run build` green (fallow default gate clean — 1 pre-existing finding, `deliverDirectMessage`); `sessions` + `auth` specs 101/101 (incl. new T40/T42/T45 coverage). Web: `pnpm typecheck` clean; `pnpm build` compiled; eslint clean on touched files; `pnpm test src/lib` 150/150. No Flutter changes (Stage E is backend+web only).

**Definition of done:** the Sessions page always shows at least the viewer's own current session; device type and IP are shown per row from real backend data, not UA-sniffing; the current session is visibly marked and its Revoke button hidden.

### 4.6 Stage F — Billing: risk mitigation + restructure

- [x] **T47** — Fix paid→paid upgrades to cancel the existing Stripe subscription before creating a new one. **Critical — financial exposure.** (F13) — `handleUpgrade` now calls `provider.cancelSubscriptionNow(oldSubId)` before `createSubscription`; a failed cancel aborts the upgrade instead of creating a duplicate. The `customer.subscription.deleted` webhook only flips to FREE when no replacement subscription is provisioned (`handleSubscriptionDeleted` guard), closing the cancel-then-create webhook race.
- [x] **T48** — Replace the 60-second time-bucketed idempotency key with a real client-supplied token (or widen the bucket + add a DB unique constraint), and pass Stripe's own `idempotencyKey` option on SDK calls. (F14) — web `StripeCardForm` mints `crypto.randomUUID()` per submit → BFF `subscribe` route → GraphQL `subscribeToPlan(idempotencyKey:)`; `StripeService.createSubscription` forwards it as the SDK idempotency option; bucket-key fallback retained for clients that omit it. Retry dedup returns the prior result without re-charging.
- [x] **T49** — Make the `invoice.paid` webhook reconcile `subscriptionTier`, not just `subscriptionPeriodEnd`/`stripeSubscriptionId`. (F15) — webhook derives the billed tier from the subscription's price (`getTierForPriceId` reverse lookup) and converges the user tier + token store when it differs.
- [x] **T50** — De-duplicate invoice rows between the sync path and the webhook path (shared reference scheme, or dedup in `getBillingHistory()`). (F16) — the subscribe path keys its first-invoice row `stripe_invoice:{latestInvoiceId}` so the webhook reconciles into it; webhook re-delivery now updates the existing row (amount/url/intent/metadata merge) instead of early-returning.
- [x] **T51** — Fix the currency double-division end to end (webhook → `WalletTransaction` → `InvoiceTable.tsx`), matching this codebase's own cents-based convention. **Critical — user-facing money bug.** (F17) — webhook stores raw `amount_paid` cents (no `/100`); `InvoiceTable.tsx` already divided by 100 for display; log line now formats dollars only for readability.
- [x] **T52** — Don't unconditionally write local cancellation state when the upstream Stripe cancel call actually failed. (F18) — `cancelSubscription` no longer swallows provider failures; the error propagates and local `cancelAtPeriodEnd` is not written.
- [x] **T53** — Fix downgrade access-cutoff timing (keep access through the paid period, don't cut immediately) and make paid→paid downgrades actually provision the replacement subscription. (F19) — paid→FREE now schedules cancel-at-period-end and keeps the tier until the `subscription.deleted` webhook flips it (fallback: immediate local change when no Stripe sub exists); paid→paid switches the price on the same subscription (`proration_behavior: create_prorations`) — replacement provisioned without a second live subscription.
- [x] **T54** — Restructure `billing/FreePageView.tsx` to use the existing `Tabs` component: Plan/Payment/Address in one tab, Invoices in another. — done: `Tabs defaultValue="plan"` with `billingPlanTab`/`invoices` triggers; plan content keeps the `xl:flex-row` layout, invoices tab renders `InvoiceTable`.
- [x] **T55** — Wrap `PlanBenefits.tsx` in the existing Radix `Accordion`, collapsed by default (no `defaultValue`). — done: single-item `type="single" collapsible` accordion, no `defaultValue`.
- [x] **T56** — (Flutter, decision-gated by §3.5) Decide whether Flutter's billing page gains a Plan Benefits section for structural parity, or keeps its current hub-page-based split. — **decided by Berkay 2026-07-31: restructure for parity.** Implemented: `lib/views/settings/billing/page_view.dart` now mirrors web's layout — TabsWidget with `Plan & Payment` / `Invoices` tabs, Plan tab stacks `_SubscriptionCard` → `_PlanBenefitsSection` → `_PaymentMethodsSection` → `_BillingAddressSection`; free-tier special-case `_FreeBillingView` removed (web renders the same tabbed layout for all tiers).
- [x] **T57** — (Flutter, only if T56 says yes) Apply the same Tabs/Accordion treatment using Flutter's existing `TabsWidget`/`AccordionWidget` — no new widgets needed either way. — done via T56: `TabsWidget` for the plan/invoices split and `AccordionWidget` (single item, collapsed by default) for the Plan Benefits list; benefits algorithm mirrors web's `PlanBenefits.tsx` (all tiers ≤ current included, next tier excluded/line-through); ARB keys `settingsPlanTab` added to en+tr; `flutter gen-l10n` regenerated.

**Stage F verification note (2026-07-31):** backend build green with the default fallow gate (no introduced findings; the one remaining new-code complexity finding `handleUpgrade` was refactored under threshold — extract `ensureStripeCustomer`/`notifyUpgrade`/`findRetryResult`/`persistUpgrade`); `npx jest src/billing` 35/35 (billing.service, stripe-webhook.controller, stripe-payment.provider, mock-payment.provider specs — the webhook and provider specs are new; the pre-existing `$transaction` mock bug in billing.service.spec was fixed); full backend suite 413 passed / 3 failed — the 3 are pre-existing `device.service.spec` failures (`prisma.device.count` unmocked, reproduced on untouched HEAD files). Web: `pnpm typecheck` clean, `pnpm build` green (Vault envs), eslint clean on all touched files, `pnpm test` 154/154. Live Stripe-mode round-trips remain deferred to the §5 gate.

**Definition of done:** no code path can create two simultaneous live Stripe subscriptions for one user; invoice amounts match what Stripe actually charged, exactly, in the UI; Invoices render in their own tab; Plan Benefits is a closed-by-default accordion.

### 4.7 Stage G — Feed comment bugs

- [x] **T58** — Fix the feed's post-detail query to include replies — switch to the backend's already-correct `postComments` query, or fix `PostService.findOne()`'s inline query (drop the `parentId:null` filter, select nested replies). (F26) — implemented 2026-07-31: `post/post.service.ts` `findOne`'s inline comments select drops `where: { parentId: null }`, adds `parentId: true` to the select, and raises `take` 20 → 100 (rows are now top-level + replies mixed; the web client already groups flat rows by `parentId`, so replies render nested with zero client shape changes). New `post/post.service.spec.ts` locks this in (asserts the query passes `{ deletedAt: null }` with `parentId` selected). The Redis `cache:post:*` TTL (60s) is invalidated on every comment create/update/delete, so the backend layer was never the poison; the browser `Cache-Control` + Next fetch-cache layers were (T59/T60).
- [x] **T59** — Pass `noCache: true` on comment/post mutation-triggered re-reads. (F27) — implemented: both GET re-read routes now pass the 5th `noCache` positional arg to `graphqlFetch` — `app/api/posts/[id]/route.ts` (post-detail re-read, the comment path) and `app/api/posts/route.ts` (feed list re-read after `["feed"]` invalidations) — bypassing the Next.js fetch cache (`cache: "no-store"`); the backend Redis cache (invalidated on mutations) still caches.
- [x] **T60** — Remove or explicitly bust (`revalidateTag`/`revalidatePath`) the BFF GET route's `Cache-Control` header from the comment mutation routes. — implemented: `Cache-Control: public, max-age=60, stale-while-revalidate=120` removed from `app/api/posts/[id]/route.ts` GET and `public, max-age=30, stale-while-revalidate=60` from `app/api/posts/route.ts` GET — the browser cache was the layer a hard reload couldn't defeat. Comment mutation routes (`app/api/comments/route.ts` POST, `[id]/route.ts` PUT/DELETE) never set cache headers — verified.
- [x] **T61** — Move the feed page's inline comment state (`PostCard.tsx`'s local `postData`) onto the same react-query key (`["posts", postId]`) the standalone post-detail page already uses correctly, so existing invalidation paths actually reach it. — implemented: `components/feed/PostCard.tsx` drops its `useState(post)` copy + direct `refreshPost` server calls; it now renders `useSuspenseQuery({ ...singlePostQueryOptions(post.id), initialData: post, staleTime: 30_000 })` and every refresh path (expand toggle, header refresh, `onCommentAdded`) is `queryClient.invalidateQueries({ queryKey: ["posts", postId] })` — the same key + invalidation the post-detail page uses, so `usePostActions().invalidate()` (both `["feed"]` and `["posts"]` prefixes) now actually reaches the feed's inline comments. `refreshPost` removed from `usePostActions` (only consumer was PostCard).
- [x] **T62** — Stop discarding `updateComment`'s mutation response; reconcile local state directly from it instead of relying on a separately-poisoned refetch. (F28) — implemented: `api/server/posts/comments.ts` `updateCommentServer` returns the BFF PUT response's `{ id, body }` (was `Promise<void>`), `usePostActions().updateComment` passes it through, and `CommentSection.handleEditComment` keeps the `localEdits` overlay set to the server-confirmed `body` on success (previously it deleted the entry and gambled on the refetch); on failure it still reverts.
- [x] **T63** — (Flutter) Widen the `postComments` GraphQL selection to request `parentId`/nested replies — the Dart model already has the field, unpopulated. (F30) — implemented: `api/server/posts/comments.dart` — `parentId` added to the list/create/update selections (and `postId` to the delete selection for T64); `create()` accepts `parentId` and `components/feed/post_card.dart` now forwards it (it was dropped on the floor — Flutter replies were broken on the write path too). Replies now round-trip and the existing `_topLevel`/`_replies` grouping in `comment_section.dart` renders them nested. While verifying the reply rendering, also fixed `comment_section.dart`'s reply-tile handlers — the recursive `_CommentTile` passed the *parent's* edit/delete callbacks to replies (`onEditStart;` was a bare statement, `onEditSave: () => onEditSave` never called, `onDelete` deleted the parent comment). Reply rendering now happens in the section state with per-reply handlers.
- [x] **T64** — (Flutter) Add explicit `ref.invalidate(postCommentsProvider(...))` after add/update/delete instead of relying entirely on the realtime push. (F31) — implemented: `api/client/posts/actions.dart` `addComment`/`updateComment`/`deleteComment` all return the server `Comment` and `ref.invalidate(postCommentsProvider(comment.postId))` after the mutation (delete now selects/returns `postId`); the realtime-push invalidate in `lib/lib/realtime/realtime_provider.dart:91` stays as the cross-device path. Also switched `actions.dart` to the `riverpod_compat` shim import per the flutter-conversion rule.

**Stage G verification note (2026-07-31):** backend `pnpm build` green with the default fallow gate (no new findings — the only post/comment findings are 4 inherited duplication hits on pre-existing `update`/`delete` lines 71–125, excluded by the inherited-findings gate; 508 analyzed). Backend `npx jest`: 414 passed / 3 failed — the 3 are the same pre-existing `device.service.spec` failures (unmocked `prisma.device.count`, reproduced on untouched HEAD). Web: `pnpm typecheck` clean, `pnpm build` green (Vault envs), eslint clean on all touched files; `pnpm test` full suite 258 passed / 60 failed where clean HEAD measures 254 passed / 64 failed — the failing set is the pre-existing React-19 `useId`-in-vitest environment issue in ui component tests (reproduced on untouched HEAD; this tree is strictly better). Flutter: `flutter analyze` clean, `dart format --set-exit-if-changed` clean, `flutter test` 437 passed / 1 failed — the 1 is the pre-existing `card_test.dart` `onTap` failure (reproduced on clean HEAD via stash). Live comment add/edit/reload round-trips (web + Flutter) deferred to the §5 live gate — no docker stack running on this server and no VAULT_TOKEN for `make up` (same blocker as Stage F/T6); all three cache layers this stage removed are covered by the route-level assertions in `post.service.spec.ts` + the removed header/noCache code paths.

**Definition of done:** a new comment (including a reply) appears immediately and survives a hard reload; an edited comment's new text persists on screen without reverting; both hold on Flutter too.

### 4.8 Stage H — Settings/General UI polish

- [x] **T65** — Remove `<ThemeToggle />` from `views/settings/general/FreePageView.tsx` — confirmed redundant with `V1Header.tsx`'s always-present one. (F20) — done 2026-07-31: removed import + usage; also dropped the now-stale "Theme" section from `settings-general.ts` page-info.
- [x] **T66** — Redesign `PageInfoButton`'s tips box to reuse the app's real `Card` component instead of ad-hoc inline classes, with a consistent width. — done: `PageInfoButton.tsx` tips box is now `<Card variant="surface" className="w-full p-3">`; all settings pages share the one component, so the box is identical everywhere.
- [x] **T67** — Add `tips` content to `settings-account.ts`/`settings-general.ts`/`settings-privacy.ts` for consistency across all 7 settings pages (or make the omission a deliberate decision, not a gap). — done: all three now define `tips` (2 each, matching api-keys/billing/sessions style); every one of the 7 settings page-info constants now has tips.
- [x] **T68** — Add a `PageInfoButton`/`settings-security.ts` pair to the Security settings page — currently the only one with no info affordance at all. — done: new `src/constants/page-info/settings-security.ts` (registered in barrel); `security/PageContent.tsx` now renders `PageHeader` + `PageInfoButton` in both the idle and enrollment states; test mock updated with `securityHeading`.
- [x] **T69** — While in the area, audit `PlanInfoCard.tsx`/`PlanAdvantages.tsx`/billing's hand-rolled `p-3`/`p-4`/`p-5` boxes for the same real-`Card`-component treatment as T66. — done: `PlanInfoCard`, `PlanAdvantages` (li items), `UpgradeActions`, billing `FreePageView` (5 boxes), `PaymentMethods` method cards all converted to `<Card variant="surface">`; smaller interactive elements (buttons, inputs, table scroll container) intentionally left as-is.

**Definition of done:** settings/general has no theme control; the info-dialog tips box looks and sizes the same on every settings page that has one; Security has one too.

**Stage H verification note (2026-07-31):** Web: `pnpm typecheck` clean, `pnpm build` green (Vault envs), eslint clean on all touched files, `vitest run` full suite 258 passed / 60 failed with `NODE_ENV=test` (the vault frontend env sets `NODE_ENV=production`, which breaks React 19's `act` — run vitest with the override; with the leaked production env it reports 169/106, i.e. the same 60 failures plus 46 `React.act is not a function` casualties). The failing 60 are the same pre-existing React-19 `useId`-in-vitest environment failures as Stage G, incl. `security/__tests__/PageContent.test.tsx` 3/3 (verified failing identically on clean HEAD via stash — the new `PageHeader`/`PageInfoButton` renders fine in the mock context). Flutter: `flutter analyze` clean, `dart format` clean, `flutter test` 437 passed / 1 failed — same single pre-existing `card_test.dart` `onTap` failure. Backend (T72): `pnpm build` green, `npx jest` 412 passed / 3 failed — the 3 are the same pre-existing `device.service.spec` failures; the deleted mock-payment spec accounts for the -1 vs Stage G's 414. Fallow: web audit verdict `fail` is driven entirely by *introduced* findings from earlier stages' uncommitted work (auth/refresh route, upload/attachment route, ChatInputBar/chat-room handlers, CommentSection) — every Stage H file's findings are `introduced: false` (inherited, excluded by the gate); the Stage G artifact was the backend run (verdict warn, inherited-only). T56/T57's `_PlanBenefitsSection` mirrors `PlanBenefits.tsx`'s tier-index algorithm and renders through the existing `AccordionWidget`; the free-tier `_FreeBillingView` special case was removed for parity (web renders the same tabbed layout for all tiers). Live round-trips for the new Flutter billing layout remain deferred to the §5 gate (no docker stack / VAULT_TOKEN on this server).

### 4.9 Stage I — Cross-cutting housekeeping

- [x] **T70** — Correct the `flutter-conversion` skill doc's stale "no token-refresh flow yet" claim. (duplicate pointer to Stage A's T7 — track once) — done via T7.
- [x] **T71** — Add the expired-session → clean-recovery regression test. (duplicate pointer to Stage A's T8 — track once) — done via T8 (`src/lib/__tests__/api-client.test.ts`).
- [x] **T72** — Either wire `MockPaymentProvider` somewhere reachable (e.g. a test/dev-mode toggle) or delete it — it exists with its own passing spec but is never referenced in real module wiring, which gives false confidence its code path is exercised. — **deleted per Berkay 2026-07-31**: removed `mock-payment.provider.ts` + its spec; `billing.module.ts`'s `StripePaymentProvider` wiring untouched (it was the only real consumer path anyway).
- [x] **T73** — Add webhook tests for `stripe-webhook.controller.ts` — zero coverage today, which is how F16 and F17 went uncaught. — done during Stage F: `stripe-webhook.controller.spec.ts` covers invoice.paid cents storage, new-invoice create, re-delivery reconciliation, tier reconciliation, non-subscription invoice handling, and the deleted-sub guard (7 tests).

---

## 5. Verify / definition of done (whole-doc gate)

Before marking this doc complete, per this project's established convention:

> **Common blocker for every unchecked box below (as of 2026-07-31):** every remaining item is a *live-environment* verification, and none of its prerequisites exist on this server — no docker stack is running (`docker ps` shows no backend/postgres/redis/WS-gateway/MinIO containers), there is no `VAULT_TOKEN` for `make up`/infra provisioning, and there are no Stripe test-mode API keys or a publicly reachable webhook URL (a headless dev server has no inbound endpoint Stripe can POST to). The implementation each box verifies is done and unit/component-tested (per-stage verification notes above); only the runtime confirmation is owed, and it must be executed on a machine with the stack + keys, not here.

- [ ] All Stage A-H task boxes checked, each stage's own "definition of done" above independently confirmed live (not just unit-tested). — **partial:** task boxes are all checked as of 2026-07-31 and every stage's verification note records its unit-level gate results (see §4.1–4.9). **Blocked:** the *live* half of this gate (T6 MFA round-trip, Stage C infra checks, Stage D upload/emoji round-trips, Stages F/G live Stripe/comment round-trips) requires the running docker stack + VAULT_TOKEN + Stripe test keys, all unavailable on this server (see common blocker above).
- [ ] Backend `pnpm build` + `pnpm test`, Flutter `flutter analyze` + `flutter test`, Web `pnpm typecheck` + `vitest run` all run fresh and their pass/fail split explicitly recomputed (not just the total) — per this project's own repeated lesson that a matching grand total is not evidence the split is right. — **partial:** every gate was run fresh per stage with explicit splits recorded — Stage H (the last work): web typecheck/build/eslint clean, vitest 258/60 with `NODE_ENV=test` (vault env leaks `NODE_ENV=production`, which breaks React 19's `act` — the override is mandatory), Flutter analyze/format clean + test 437/1, backend build green + jest 412/3 (the 60 web, 1 Flutter, 3 backend failures are all pre-existing, reproduced on clean HEAD). **Blocked:** a single fresh final recompute on the committed end-state is owed per convention and deferred to the live gate — it needs the vault envs + `DATABASE_URL` + the `NODE_ENV=test` override, and its failure deltas must be re-checked against HEAD, which is only meaningful on the machine that will also run the live round-trips.
- [ ] A live round-trip for each of: friend-request-while-on-messages, chat-room send-and-scroll, avatar upload, chat-nickname save-and-see-in-chat-room, sessions list on a real logged-in session, a real Stripe test-mode upgrade-then-downgrade, feed comment add/edit/reload, and at least one real reproduction of a stale-session redirect (forced via a tier change, per F23a, since it's the easiest trigger to produce on demand). — **partial:** each flow is implemented and covered by unit/component tests — friend-request `emitToService` (T9–T11), chat-room sentinel/scroll (T12), avatar upload (T29–T31), chat-nickname persist+thread (T13–T16 + specs), sessions list + current-session badge (T39–T42/T45, auth+sessions specs 101/101), Stripe upgrade/downgrade (Stage F, billing specs 35/35 incl. 7 webhook tests), feed comments (Stage G, post specs + route assertions), stale-session redirect (T8's 6 api-client regression tests + T1–T5). **Blocked:** a live round-trip requires the full running stack (backend + Redis/Postgres + WS gateway + MinIO for uploads) so cookies, sockets, and webhooks behave as in production; none of those containers are up on this server (see common blocker above).
- [ ] Stage D live round-trips: send a message with an image attachment and one with a PDF/docx from both `/v1/en/messages` and `/v1/en/chat-room` on web and Flutter, confirm the recipient renders the thumbnail/chip and opens the original in a new tab/external app; open the emoji picker from both composers on both platforms and confirm insert-at-cursor. Requires the §5 reverse-proxy check on `/api/uploads/*` → MinIO (deferred from Stage C, T23) to pass first. — **partial:** the upload path is fully implemented repo-side — `POST /upload/attachment` endpoint with allowed-type + magic-byte-skip handling (T29), web BFF route + constants (T30), thread-through of attachments to both messaging services (T31) — and covered by the Stage D verification note's unit results. **Blocked:** the end-to-end round-trip hard-depends on T23's live reverse-proxy check (`app.eys.gen.tr/api/uploads/*` → MinIO), which itself requires the running docker stack + Vault access; the emoji insert-at-cursor check needs a live browser against the real backend — none of this is possible on this server (see common blocker above).
- [ ] Stage F live round-trips (Stripe test mode): paid→paid upgrade cancels the old live subscription (Stripe dashboard shows exactly one active sub per customer) and the Billing tab shows the new tier; downgrade to FREE keeps access through the billing period then flips to FREE; paid→paid downgrade provisions the prorated price on the same subscription; invoice amounts in the Invoices tab match Stripe's cents exactly; duplicate webhook delivery (replay a `invoice.paid` event) updates the existing row rather than adding a second. — **partial:** every scenario is implemented and unit-covered — cancel-on-upgrade (T47/T48), downgrade period-keep + paid→paid price-switch with proration (T53), invoice cents + webhook reconciliation/dedup (T49/T50, `stripe-webhook.controller.spec.ts` 7 tests incl. re-delivery) — Stage F verification note: billing specs 35/35. **Blocked:** real Stripe test-mode verification needs live API keys and a publicly reachable webhook endpoint (Stripe must be able to POST `invoice.paid` events back); this headless server has neither, and the Stripe dashboard cross-check is by definition external (see common blocker above).
- [ ] A dedicated verification round (mirroring this series' established pattern) before treating any Stage as done — implementation claims in this project have repeatedly overstated completion; do not skip re-checking against live code once fixes land. — **not started, by design:** this is a process gate, not an implementation task — it can only be executed as the closing round once the boxes above (323–327) have been satisfied in a live environment, so pre-marking it would violate exactly the "don't overstate completion" discipline it demands. Deferred to the same live run as 323–327.

---

## 6. Queue for next phase

No further reported issues beyond the 8 covered here. Next phase should be scoped from whatever Berkay reports after this doc's fixes are implemented and verified — per this project's convention, do not start drafting `convert-frontend-11-flutter.md` until this one's tasks are checked off and its verify section (§5) passes.
